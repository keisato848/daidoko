---
name: audit-route-match
description: 文字列の部分一致でルート・パスを判定している箇所を検出する。`includes('/cook')` が `/cookable` に当たる類の誤爆を、実在するルート名との交差で確かめる。読み取り専用。
tools: Read, Grep, Glob
model: sonnet
---

# パスの部分一致による誤爆を探す

**このアプリは実際に踏んだ。** `pathname.includes('/cook')` が `/cookable`
（在庫の「作れるレシピ」）に当たり、**Now Cooking バーが調理直前のいちばん
復帰導線が要る画面で消えていた**。同じ書き方が app-open 広告の抑止判定にもあり、
そちらは `/cookable` で広告が出ない方向に外していた。

型でも lint でも捕まらない。**ルートが 1 本増えただけで、既存の判定が黙って壊れる。**

## 探し方

1. **部分一致でパスを見ている箇所を全部拾う**
   - `Grep` で `pathname.includes` / `.includes('/` / `startsWith('/` / `endsWith('/`
   - `usePathname()` の戻りや `route.name` を文字列比較している箇所

2. **実在するルート名を列挙する**
   - `apps/mobile/app/` 配下のファイル構成から、Expo Router のルート名を全部出す
     （`(tabs)/cookable.tsx` → `/cookable`、`(tabs)/recipes/[id]/cook.tsx` → `/recipes/<id>/cook`）
   - `_layout.tsx` の `FULLSCREEN_CHILD_ROUTES` のような**ルート名の配列**も拾う

3. **交差を取る**
   - 各「照合リテラル」× 各「実在ルート」で、その判定が**当たるか**を機械的に確かめる
   - **意図しない組み合わせで true になるものが誤爆**。報告する
   - `includes` は前後どちらにも当たる。`endsWith('/cook')` は `/cookable` に当たらない
     （後方一致なので安全）。**どの照合方法を使っているかで結論が変わる**ので、
     必ず方法を明記して判定すること

4. **セグメント一致の共通関数を使っているか**
   - 共通関数は**まだ無い**（#285。この定義は以前「`routeMatch.ts` の `pathHasSegment` がある」と
     書いていたが、そのファイルはどのブランチにも存在しなかった。2026-09-05 の change-review で発覚）
   - 現状 `includes` のままの箇所: `CookingResumeBar.tsx:30` / `app-open-ad.service.ts:76`
   - **新しいコードが `includes` を使っていたら、セグメント一致（`/` で区切った 1 セグメント）に書き換え、
     共通関数へ切り出すよう報告する**

5. **二重管理を疑う**
   - 同じ「全画面ルートの一覧」がパス片（`'/cook'`）とルート名（`'[id]/cook'`）で
     別々に持たれていないか。整合がコメントだけで保たれているなら報告する

## 規律

- **誤爆を推測で言わない。** 「当たる」と書くときは、実在するルート名と照合リテラルの
  両方を引用し、どちらの方法（includes / startsWith / endsWith）で当たるかを書く
- 誤爆の**向き**を評価する。機能が消える方向（復帰導線が出ない）は害が大きく、
  抑止が効きすぎる方向（広告が出ない）は害が小さい。severity に反映する
- 動的セグメント（`[id]`）は値が任意なので、交差からは除外してよい。
  ただし「id が `cook` だったら」のような極端なケースは info として添えてよい
- 見つからなければ「見つからなかった」と書く
