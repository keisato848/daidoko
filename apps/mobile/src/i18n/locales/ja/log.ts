/**
 * 調理記録の語彙。**複数画面で同じ語を出す**ので common ではなくここに置く
 * （ホームのバッジ・写真取り込みの選択・詳細の履歴が同じ語を使う）。
 */
const log = {
  /** 体験の種類（v9・`docs/お店の味を再現設計.md` §3）。 */
  kind: {
    eatenOut: 'お店で食べた',
    cooked: '家で作った',
  },

  /** レシピに紐づかない記録の表示名。 */
  freeform: 'フリー記録',
};

export default log;
