# ブラウザ操作セッションへの依頼（2026-08-23）

> このファイルは**別セッション（Claude in Chrome 接続あり）に渡すプロンプト**です。
> 作業が終わったらこのファイルは削除してよい（結果は各所へ反映済みのはず）。
> 実行者向けの前提・境界は `.claude/skills/console-browser-ops/SKILL.md` を必ず先に読むこと。

## 全体の背景（3 行）

- 「だいどこ」は Play 公開中（1.10.1/10026）・App Store 公開中（1.10.2）。クラウド同期を含む **1.11.0** を近く両ストアへ出す
- 同じ AdMob アカウント（`pub-2633806931583277`）に、別アプリ「さいえん手帳」も入っている
- **同期リリースの唯一のブロッカーが下の A（データセーフティ申告）**。B・C は付随作業

---

## A. Play Console — データセーフティ申告の更新【最優先・リリースのブロッカー】

**なぜ必要か**: 現行申告は「収集はすべて一時的処理（解析後破棄）・サーバー保持なし・データ削除リクエスト=いいえ」。
クラウド同期はレシピ・買い物・在庫をサーバー（PostgreSQL）に**保持する**ので、この前提が崩れる。
申告と実装の不一致は審査で弾かれるため、**アプリを提出する前に**申告を変更し審査に通す必要がある。

**場所**: Play Console → だいどこ（`com.daidoko.app`）→ アプリのコンテンツ → データセーフティ
（URL 直行可: `.../app-content/data-privacy-security`）

**現行申告（2026-07-05 送信・変更前の状態）**

| 項目                 | 現在                                                                 |
| -------------------- | -------------------------------------------------------------------- |
| データの種類         | ウェブ閲覧履歴（URL 取り込み）／写真／その他のユーザー作成コンテンツ |
| 収集・共有           | いずれも「収集」のみ（共有なし）                                     |
| 処理                 | **すべてエフェメラル（一時的処理）**                                 |
| 必須/任意            | 任意                                                                 |
| 目的                 | アプリの機能                                                         |
| 暗号化               | はい                                                                 |
| データ削除リクエスト | **いいえ**                                                           |
| アカウント作成       | なし                                                                 |
| 広告 ID              | 使用する（2026-07 の広告有効化で申告済み）                           |

**変更する点（ここだけ直す。他は触らない）**

1. **「その他のユーザー作成コンテンツ」**（レシピ・買い物リスト・在庫が該当）
   → **エフェメラルのチェックを外す**（サーバーに保持されるため）
   → 目的は「アプリの機能」のまま
2. **「写真」**
   → **エフェメラルのままでよい**。クラウド同期は写真を送らない（S3 は次のリリース）。
   ※ Web 共有では写真をサーバーに保存するが、これは 2026-08-14 に申告済み（「一時的に処理」を いいえ に変更済み）。
   **画面上で写真が既に「エフェメラルでない」状態なら、そのまま触らない**
3. **「デバイスまたはその他の ID」を追加**
   → 収集する／共有しない／**必須**／目的=**アプリの機能**／エフェメラルではない
   （同期は端末ごとのランダム ID と Expo Push トークンをサーバーに保持する。氏名・メールは一切収集しない）
4. **データ削除リクエスト = はい**
   → 削除方法の説明: アプリ内の「家族グループ」画面から
   「共有グループを削除」（サーバー上の共有データを全消去）または「このグループから離脱」で行える
   → 削除リクエスト用 URL を求められた場合はプライバシーポリシーの gist URL を入れる:
   `https://gist.github.com/keisato848/49fb9a73f3a1c9952548388f288c383c`
5. **アカウント作成 = なし のまま**（アカウントレス設計。氏名・メール・電話番号は収集しない）
6. **暗号化 = はい のまま**（HTTPS）

**手順**

1. 5 ステップのウィザードを進める: 概要 → 収集とセキュリティ → データの種類 → 使用と処理 → プレビュー
2. **各ステップの画面を都度ユーザーに見せて確認を取る**
3. 保存しただけでは審査に載らない。**「公開の概要」→「変更を審査に送信」まで実行**
   → ここは**ユーザーの明示承認が必要**（`console-browser-ops` §1 の承認ゲート）

**注意**: プライバシーポリシーの gist は**まだ更新されていない**（原本 `docs/privacy-policy.md` は更新済みだが、
main へのマージ待ち）。**申告の送信は gist 更新後**にするのが望ましい。順序が前後しそうならユーザーに確認すること。

**終わったら**: `docs/リリース手順.md` §4 の「現行申告」の記述を新しい内容に更新する（次に見る人のため）。

---

## B. App Store Connect — さいえん手帳の「マーケティング URL」を登録

**なぜ必要か**: さいえん手帳（iOS・`com.saientecho.app`・App ID `6801141151`）は
**デベロッパー Web サイトが未登録**（iTunes Lookup API の `sellerUrl` が空）。
AdMob はストア掲載のデベロッパー Web サイトを見て app-ads.txt をクロールするため、
未登録だとクロール対象が無く、アプリ確認が永久に完了しない。

**場所**: App Store Connect → さいえん手帳 → App 情報（または該当バージョン）→ **マーケティング URL**

**入れる値**: `https://keisato848.github.io/`
（だいどこ iOS は `https://keisato848.github.io/daidoko/` が入っている。
さいえん手帳の LP があるならそのパスでよい。無ければルートで可）

**注意**: 公開中バージョンのメタデータ変更は審査対象になる場合がある。
「App 情報」側の変更で済むか、次バージョンでの反映になるかを画面で確認し、**ユーザーに判断を仰ぐこと**。

---

## C. AdMob — app-ads.txt の再クロール（待つだけ・急がない）

**現状**: だいどこ iOS / さいえん手帳（Android・iOS）で
「app-ads.txt ファイルが設定されている可能性がありますが、お客様の詳細情報が AdMob アカウントの情報と一致しません」
が出ている。**別セッションの調査で「単なるクロール待ち」と結論済み**。

**確認済みの事実（再調査は不要）**

- `https://keisato848.github.io/app-ads.txt` は AdsBot-Google / Googlebot のどちらでも **200・text/plain**
- 中身は AdMob が提示するスニペットと 1 バイト違わず一致（BOM・CRLF なし）:
  `google.com, pub-2633806931583277, DIRECT, f08c47fec0942fa0`
- `robots.txt` は 404（クロール制限なし）
- だいどこ Android は同じドメインで**検証済み**

**やること**: 各アプリの「app-ads.txt のステータス」で **「アップデートを確認」を押すだけ**。
反映に最大 24 時間かかる。押しても状態が変わらなくても、**それ以上の調査や設定変更はしないこと**
（ファイル側は正常であることが確認済み）。

**重要**: だいどこ iOS は**ストアリンクの登録自体は成功済み**（2026-08-23）。
app-ads.txt の警告はプログラマティック需要に関わるもので、広告配信を完全に止めるものではない。

---

## やらないこと（境界）

- **Google Payments 販売アカウントの送信**（方針A の間は絶対にしない。自宅住所公開・不可逆）
- 法的契約への同意、アカウント作成・ログイン、カード/銀行/税務情報の入力
- 秘密鍵ファイルのアップロード
- 上記 A〜C 以外のコンソール設定の変更

---

## 実行結果（2026-08-23・さいえん手帳セッションが CDP Chrome で実施）

### A — ✅ **審査に送信済み（2026-08-23 19:20 ごろ・「審査中の変更」）**。gist も 2026-08-22 版（§3.5／§5.1 入り）に更新済み。`docs/リリース手順.md` §4 の「現行申告」を書き換えた

画面の実態が上の「現行申告」表と 2 点ずれていた（7 月の広告申告・8/14 の Web 共有申告が反映されていない）。
結果が同じになるように最小限だけ触った:

| 項目                                                     | 画面の実態（変更前）                                                               | 操作                                                 |
| -------------------------------------------------------- | ---------------------------------------------------------------------------------- | ---------------------------------------------------- |
| その他のユーザー作成コンテンツ                           | **既に**「一時的に処理しない」・収集のみ・任意・アプリの機能                       | 変更なし                                             |
| 写真                                                     | 既に「一時的に処理しない」・収集のみ・任意・アプリの機能                           | 変更なし                                             |
| デバイスまたはその他の ID                                | **既に申告済み**: 収集＋**共有**・必須・非エフェメラル・目的=広告（収集/共有とも） | **収集目的に「アプリの機能」を追加**。共有側は触らず |
| データ削除リクエスト                                     | いいえ                                                                             | **はい** ＋ データ削除用 URL = gist（`49fb9a73…`）   |
| 暗号化 / アカウント作成 / ウェブ閲覧履歴（エフェメラル） | そのまま                                                                           | 変更なし                                             |

プレビュー: 共有=デバイス ID／収集=写真・その他のユーザー作成コンテンツ・デバイス ID／削除=gist URL。
「公開の概要」に **データ セーフティ 1 件が未送信**で載っている（クイックチェック実行中）。

**送信前の条件:** gist は 2026-08-08 版のまま（§3.5／§5.1 なし・PR #211 未マージ）。Play は削除用 URL に
「削除手順が目立つ・データ種類と保持期間の明記」を要求するので、**gist 更新 → 送信**の順にする。

**コード裏取り（4 読み手 + 5 反駁）で分かったこと — 申告フォームは変更不要だが、文書側に直すべき点:**

- ✅ 本文（レシピ・買い物・在庫）は `sync_entities.payload` に保持・写真は同期で送らない・アカウント無し — 申告どおり
- ⚠️ 「デバイス ID を**共有しない**」は誤り。同じビルドに AdMob SDK が入り広告 ID を共有しているので、
  Play の種別は 1 エントリ＝「収集＋共有」のまま（画面の既存回答が正しい）
- ⚠️ **「このグループから離脱」はデータ削除ではない**（`leaveGroup` は `sync_devices` の自分の行だけ削除。
  自分が push した本文・`updated_by_device` の端末 ID はグループ存続中は残る）。サーバー上のデータを消せるのは
  オーナーの「共有グループを削除」（CASCADE）だけ。ポリシー §5.1／リリース手順 §4 の文言は「離脱＝端末を外す」に留めること
- ⚠️ tombstone・archived レシピは無期限に残る（GC 未実装・設計書の未決事項）。ポリシーの「削除された記録は残る」は整合
- ⚠️ Web 共有の停止は soft delete（`revoked_at`）で写真 BLOB と本文は残る。ポリシー §3.4「共有ページは削除されます」は
  閲覧不可の意味にとどまる — 文言か実装のどちらかを寄せる
- ℹ️ リリース手順 §4 の「広告ID=不使用」「全タイプがエフェメラル」は古い（7 月・8/14 の申告を反映していない）

### B — 追加作業なし（前セッションで対応済み）

ASC に「App 情報」レベルのマーケティング URL は無く**バージョン単位**。公開中 1.0 は PATCH で 409、
**審査中の 1.1.0 に `https://keisato848.github.io` 設定済み**（API で確認）。1.1.0 公開で `sellerUrl` に反映される。

### C — 3 アプリとも「アップデートを確認」を押した（状態は変わらず＝未クロール）

AdMob「app-ads.txt」タブの実測: だいどこ Android は `keisato848.github.io/app-ads.txt` を **14 分前にクロールして確認済み**。
さいえん Android／だいどこ iOS は行なし＝未クロール。さいえん iOS は「デベロッパー ウェブサイトが見つかりません」（B 待ち）。
**「github.io は PSL 共有ドメインなので使えない」説は実データと矛盾する** — 独自ドメインは不要。
