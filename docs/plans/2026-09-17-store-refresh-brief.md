# ストア掲載・動画・スクリーンショット刷新の検討 — 別セッション用ブリーフ（2026-09-17）

このファイルは、実装セッション（`daidoko-8b`）から**別の Claude セッションへ検討を切り出す**ための引き継ぎ。
読む側は本文の前提知識を持たない前提で書いてある。**最初にこのファイルを全文読み、次に「やってほしいこと」の順に進める。**

## 依頼（本人の言葉・そのまま）

> 設計・実装は引き続きお願いしたいのだが、これらの機能を実現すると、このアプリは家事体験を刷新できるようなアプリケーションとしての機能を保持することになると思う。
> そうした場合に、肝になってくるのはやはりストア情報やストアの動画・スクリーンショットである。
> Gemini側でOmni等を定額プラン内で使うなど、過去のリリースではできなかったこともできるようになるはず。
> 議論が錯綜しないように、これらを実現するための検討を別のClaudeセッションで立ち上げてください。

「Omni」が何を指すかは**推測しない**。定額プラン（Google AI Pro）で実際に使える生成機能を `agy models` 等の実物で確かめる（下記 1）。

## なぜ別セッションか

実装セッション（`daidoko-8b`）は Phase 2（相談のエージェント化・献立の家族共有・ウィジェット・一括生成の非同期化）の実装を進めている。
ストアの話を同じ会話に混ぜると議論が錯綜する、という本人の判断。**このセッションは apps/ のコードを触らない**。

## 前提知識（実装セッションから引き継ぐ事実）

### 位置づけ

- 提供価値は「**料理という家事を丸ごと**」（散らからないことが本質。「お店の味を再現」は入口の一機能）。本人は今回の機能群で「**家事体験を刷新できるアプリ**」になると見ている
- ペルソナ けんじ（比較ブロガー）の指摘: 「AI に聞ける」と書いて料理に答えないのは**掲載事故**。「何でもできる」と読まれると別の期待外し

### 実装中の機能（＝掲載で見せたいもの。実機の画面はまだ無い）

受付票: `docs/plans/2026-09-16-consult-agent-and-menu-sharing.md`（合意済み・Phase 0 Go・Phase 2 進行中）

- 相談のエージェント化 v1: 「玉ねぎ切らしてる」→ 確認カード → 買い物リストに足す（取り消し Toast）。候補数（既定 3）。使い方の質問に相談の中で答える
- 相談の送信改善: 写真 4 枚まで・2 往復目以降は写真の代わりに AI の読み取りテキストを送る（低速回線でもタイムアウトしない）
- 献立の家族共有: 家族で 1 つの献立、1 食＝主菜＋副菜・汁物（枠はユーザー設定）、週ビュー（案 1・縦 7 行）、作ったら「済み」、調理記録も同期
- 一括 AI 生成の枠対応（主菜・副菜・汁物）＋バックグラウンド化＋完了 push
- ウィジェット 3 段（今日の全品／週の主菜／週の主菜＋副菜）。iOS も（iPhone は 2 段）

**スクショは実装後にしか撮れない。** 今できるのは、掲載文の骨子・見せる機能の順番・動画の台本・生成手段の検討・モック。順序を間違えない（`docs/reviews/persona-week-view-2026-09-16.md` に週ビューの ASCII モックあり）。

### 現在のストア資産と道具

- 掲載文: `docs/store/listing-ja.md` / `listing-en.md`。反映は Skill `update-store-listing`（androidpublisher API・**公開反映は利用者承認必須**）
- スクショ: `docs/store/phone-screenshots{,-en}/`（エミュレータから言語ごとに機械で再取得できる）。フィーチャーグラフィック `scripts/release/generate-play-promos.mjs`
- プロモ動画: Skill `promo-video`（実機/エミュレータから自動収録・編集。見せるデータは `seed.ts` / `scripts/release/promo-assets/` にコード化）。現行は YouTube `https://www.youtube.com/watch?v=XsiIi-8pEX8`（公開・Play に登録済み）
- 役: `store-ops`（掲載素材の取得・検証・dry-run まで。公開は司令塔＋承認）、`audit-listing-claims`（掲載文の主張を実装と突き合わせる。**#265: 主張 30 件が裏取りできない前例**）、`persona-review`（6 人。5 人＝使い勝手、けんじ＝価値と競合）、`growth-analyst`（数字。**無ければ「無い」と言う**）、`release-notes-drafter`
- iOS: App Store も同じ掲載文体系（ja/en）。`docs/store/` 配下に ASC 用の資材あり。1.13.2 が READY_FOR_SALE

### 流入の実態（数字は実物で確かめること）

- `docs/growth/流入の記録.md`（DL 数・広告・施策の判断ログ）、`docs/store/なぜインストールされないか-2026-08-26.md`
- 要点: インストールはほぼ本人のみ・律速は転換率でなくリーチ・ASO（アプリ名・スクショ順・アイコン・タグ・フィーチャーグラフィック）は 2026-07 に実施済み・Google Ads は「低コストが成立しない」で見送り・Apple Ads の $100 クレジットあり

### 掲載の掟（守る）

- **主張は実装で裏が取れるものだけ**（`audit-listing-claims` を通す）。実装前の機能を「ある」と書かない
- データセーフティ申告（Play）・プライバシーポリシーとの整合。家族共有・サーバー保存の説明が変わるなら申告も変わる
- **公開反映（API アップロード・YouTube 差し替え・データセーフティ）は利用者の承認が要る**。dry-run まで
- 数字を作らない。DL 数・評価数は `growth-analyst` が実物を読む

### Gemini の生成機能（統合ハーネスの方針）

- API キーは使わない。Antigravity CLI（`agy`）の Google アカウント上限だけ（`useG1Credits: false`）。`harness:gemini-task` / `harness:gemini-review` が agy 経由
- 現状のモード: `geminiwrite`（`.claude/settings.json`。実装は write 役、Claude は検査。**ただしこのモードはまだ振る舞いを変えない**＝表示と役のモデル選択だけ）
- **画像・動画・音声の生成が agy から呼べるか、定額プランの範囲か、は未確認。** ここが本人の期待の中心なので最初に確かめる

## やってほしいこと（順に）

1. **生成機能の実物確認**: `agy models`・`agy --help`・公式の案内で、定額プラン内で使える生成（画像・動画・音声・字幕）を列挙する。使えないものは「使えない」と書く。結果を `docs/store/gemini-generation-2026-09-17.md` に残す
2. **ヒアリング**（`harness:refine-requirements`）: 本人に、実際に困ったこと・どの画面を見せたいか・何を約束しないか・動画の長さや言語を聞く。記録は `docs/reviews/hearing-store-2026-09-17.md`（`hearing-2026-09-16.md` と衝突させない）
3. **受付票**（`harness:intake`）: `docs/plans/2026-09-17-store-refresh.md`。受入基準・スコープ外・Phase。**Phase 2（素材の実制作）は実装セッションの画面が出来てから**
4. 案を出す: 掲載文 ja/en の骨子・スクショ構成（機能ごと・撮る画面のリスト・撮影は実装後）・動画台本・生成手段。`persona-review` に回して 6 人の目を通す
5. 分かったことは `docs/store/` か `docs/growth/流入の記録.md` に**症状→原因→対処**で残す（CLAUDE.md §5）

## やらないこと

- `apps/` `packages/` `scripts/`（release 系を除く）のコード変更。Phase 2 は実装セッションが担当
- ストアへの公開反映・YouTube の差し替え・データセーフティの送信（承認前）
- 実装前の機能をスクショ・掲載文で「ある」と見せること
- 数字の推測

## 連携

- 実装セッション: **`daidoko-8b`**（`ListAgents` で見える。`SendMessage` で連絡）。決めるのは利用者
- 作業ブランチ: `docs/store-refresh-2026-09`（推奨）。同じ作業ディレクトリを共有しない（`docs/開発ハーネス.md` §7-0 の 5）。worktree で動く
- 書いたものは push して PR にする（マージは利用者判断）
