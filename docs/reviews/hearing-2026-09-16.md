# ヒアリング記録: 相談のタイムアウト修正と、その先のエージェント化（2026-09-16）

## きっかけ・方法・回答者

きっかけ: 2026-09-16 の Gemini レビューで「相談は往復ごとに写真をフル再送しており、低速回線では 45 秒に収まらない」
と判明した。**その直後に私（Claude）が要件を決めずに対処案を Issue #342 と docs へ書き、利用者から
「勝手に判断するな。修正後の要件が決まっていないだろう」と止められた。**

方法: 選択式 4 往復＋割り込みの自由発言 3 件。回答者は本人（オーナー・1 人）。
**1 人の証言なので一般化に注意。**

---

## 分かったこと `F`

### 直したい振る舞いについて

- **F1**: あるべき挙動は「**写真を減らして速く返す**」。
- **F2**: その中身は「**2 往復目以降は同じ写真を送り直さない**」。
- **F3**: 判断は「**アプリが常にそうする**」。回線速度で分岐させない。
- **F4**: 適用範囲は「**相談だけ**」。
- **F8**: サーバーが写真を会話の間持つ案には「**保持は避けたい**」。
- **F13**: 「**クライアントからまとめて送るようにすればいいのでは？**」→ 確認したところ
  「1 メッセージで 4 枚まとめて添付」と「読み取り結果で代用」の**両方やる**。

### 実際の使い方（仮定ではなく実績）

- **F5**: 相談で「**3〜4 枚使う**」。
  実装で裏取り: `MAX_CONSULT_IMAGES_PER_MESSAGE` により**1 メッセージ 2 枚が上限**で、添付ボタンが
  無効化される（`apps/mobile/app/(tabs)/recipes/consult.tsx:427`）。**3〜4 枚使う人は必ず 2 回以上に
  分けさせられており、2 往復目には「新しい写真」が乗る。** 再送だけの問題ではなかった。
- **F6**: 9/13 にタイムアウトした後は「**その場で相談をやめた**」。＝ 再試行せず離脱している。
- **F7**: 9/13 の回線は「**覚えていない**」。→ **未解決のまま置く**（推測で埋めない）。

### 範囲について

- **F9**: 初回送信（3〜4 枚で 1.4〜2.0MB）は「**対象外でよい**」。1 往復目は実際に通っていたため。
- **F10**: #335（原寸デコード 3 回・画像 5 経路が遅い）は「**両方。ただし OCR 品質は絶対に変えない**」
  （速度とメモリの両方が目的、ただし読み取り精度が落ちるならやらない）。
- **F11**: #334 同梱の非タイムアウト項目のうち認めるのは「**BYOK とサーバー経路の非対称を揃える**」
  「**pantry の 200 件切りをサーバー経路にも入れる**」の 2 件。共通入口 `image-payload.ts` への寄せは
  選択肢に出したが**選ばれなかった**。
- **F12**: 私が勝手に作った Issue #342 は「**閉じて #331 に統合する**」。

### 目的そのものについて（最重要）

- **F14**: 「**相談機能をエージェントとして○○を買い物リストに追加のように使えるようにするって要望は
  どこに行った？**」→ **私が落としていた。** 実物は PR #330 の `docs/相談してレシピを作る設計.md` §7 に
  「2026-09-14 確定・実装前」として存在し、**その PR を私が止めていた**。
- **F15**: 「**タイムアウトだけが修正必要な要件じゃないからね**」。

---

## 詰まりの構造

設計書 §7-9 に依存が明記されている。

| 段階     | 中身                                                               | 前提 |
| -------- | ------------------------------------------------------------------ | ---- |
| **前提** | **#331（相談のタイムアウト）の解消**                               | —    |
| 別 Epic  | 取り消し基盤（単独で出荷できる）                                   | —    |
| 1〜7     | エージェント化（操作カタログ・解決層・`actions[]`・確認カード・…） | #331 |

つまり **#331 は目的ではなく関門**。オーナーが求めているのは §7-1 の

> Gemini や Claude に「レシピを教えて」と聞くだけだと、**それは何も残らない**。買い物リストも作ってくれない。

の解消であって、タイムアウトの秒数ではない。**このセッションで私はその順序を報告せず、
関門を目的のように扱った。**

さらに F6（タイムアウトしたらその場でやめる）を踏まえると、関門が開かない限り
「使ってみて価値を感じる」段階に到達しない。

---

## 設計に渡せる要求 `R`

| #      | 要求                                                                                                                      | 根拠        |
| ------ | ------------------------------------------------------------------------------------------------------------------------- | ----------- |
| **R1** | 相談の 2 往復目以降は同じ写真を送り直さず、「AI が読み取った内容」を代わりに送る。**サーバーは写真を保持しない**          | F2・F8・F13 |
| **R2** | 1 メッセージで 4 枚まとめて添付できるようにする（現在の 1 メッセージ 2 枚上限を見直す）                                   | F13・F5     |
| **R3** | アプリが常にそうする。**回線速度で分岐する仕組みを作らない**                                                              | F3          |
| **R4** | 適用は相談のみ。他経路のタイムアウトは今回そろえない                                                                      | F4          |
| **R5** | BYOK 経路とサーバー経路のタイマー開始位置を揃える                                                                         | F11         |
| **R6** | pantry の 200 件切りをサーバー経路にも入れる                                                                              | F11         |
| **R7** | #335 は速度とメモリを両方直す。**ただし OCR 品質を変えない**（`width`/`height` の意味を変えない・手動 mock に歯を入れる） | F10         |
| **R8** | Issue #342 を閉じて #331 に統合する                                                                                       | F12         |
| **R9** | #331 が解消したら §7（エージェント化）へ進む。**#331 は目的ではなく前提**                                                 | F14・§7-9   |

---

## 要求されなかったこと

**ここに書いてあるものを次のセッションが設計し直さないこと。**

- **回線速度による分岐**（速いときは今のまま／遅いときだけ減らす）… F3 で「アプリが常にそうする」が選ばれた
- **初回送信の軽量化**… F9 で「対象外でよい」
- **相談以外の経路のタイムアウト統一**… F4 で「相談だけ」
- **共通入口 `image-payload.ts` への寄せ**… 選択肢に出したが選ばれなかった
- **サーバー側での写真保持**… F8 で明示的に「避けたい」
- **「128kbps で通ること」を合格条件にすること**… **一度も要求されていない。私が勝手に置いた指標**
  （docs §4-0c と Issue #342 に書いてしまった。撤回対象）
- **利用者に選ばせる UI**（「写真を小さくして送りますか」）… F3 で「アプリが常に」が選ばれた

---

## 優先順位

1. **R1・R2**（送信量を減らす）… #331 の関門を開ける本体
2. **R5・R6**（非対称と pantry）… 同じ PR に同梱済みで、追加コストが小さい
3. **R8**（Issue 整理）… すぐできる
4. **R7**（#335）… 画像 5 経路に効くが、OCR 品質の確認が要るので独立して進める
5. **R9**（§7 エージェント化）… 1 が終わってから。別 Epic の「取り消し基盤」は独立して着手できる

---

## まだ聞けていないこと

- **9/13 の回線**（F7「覚えていない」）。**どの速度帯で壊れたのかは分かっていない**
- **R1 の品質**: 「AI が読み取った内容」で写真の代用が成り立つか。**未検証**
- **「速く返す」の目安秒数**。今回は聞いていない
- **§7-10 の 4 件**（設計書に「勝手に決めない」と明記）: 入口の変更 / 家族への説明 / 取り消しの範囲 / 掲載文
- **PR #337/#339/#338 の扱い**。要件が固まったので再判断が要る（F15 の「要件を決めてから」の対象）

---

## 私（Claude）の失敗として記録すること

1. **要件を決めずに対処案を書いた**。Gemini の指摘を受けた直後、Issue #342 に「画像 ID で参照する設計へ」
   「タイムアウト値を見直す」と書き、docs §4-0c に「128kbps を合格条件にする」と書いた。
   **どれも本人に確認していない。** `harness:refine-requirements` §2-1 そのもの。
2. **目的と関門を取り違えた**。#331 は §7 の前提にすぎないのに、それ自体を目的として報告し続け、
   本命のエージェント化（F14）が止まっていることを報告しなかった。
3. **「相談だけ」で済むと思い込んだ**。F5 を聞いて初めて、1 メッセージ 2 枚上限のせいで
   3〜4 枚使う人が必ず分割させられている（＝再送以外の原因がある）と分かった。
   **先にコードを読んでいれば質問が具体的になった**（§2-3）。

---

---

# 第 2 部: エージェント化の要件（同日・続き）

## きっかけ・方法・回答者

利用者「**エージェント化の要件が決まらない限り先には進めない。**」
設計書 `docs/相談してレシピを作る設計.md` §7（PR #330・未マージ）は「2026-09-14 確定」としつつ §7-10 に 4 件を未決で残していた。
また §7-4 の「確認カードをタップしてから実行」は**ご本人の発言からではなく設計側が置いたもの**だったので、そこから確認した。

方法: 選択式 3 往復＋復唱 1 往復＋割り込みの自由発言 1 件。回答者は本人（1 人）。**一般化に注意。**

## 分かったこと `F`

### 実行の形

- **F16**: 「○○を買い物リストに追加」と言ったときの動き → 「**確認してから実行でよい**」。§7-4 はこれで本人確認が取れた。

### 実際にあった場面（直近 1〜2 週間）

- **F17**: 相談の途中で残したい／やってほしいと思ったのは「**買い物リストに足したかった／今日の献立として決めたかった／
  作った記録を残したかった／在庫を減らしたかった・使い切ったと言いたかった／アプリケーションの使い方を教えてほしかった**」の 5 つ。
- **F18**: いちばん回数が多く困ったのは「**買い物リストに足す**」。
- **F19**: 「使い方」の中身は「**どの画面でやるのか分からなかった／その機能があるか自体分からなかった**」。料理の質問ではない。
  実装で裏取り: 各画面に「この画面の使い方を表示」ボタンは既にある（`ui.help.label`）が、**場所と機能の有無**は画面単位のヘルプでは答えられない。
- **F20**: 使い方を答える場所は「**相談の中で答えてくれればよい**」。
  → PR #330 が中止した「AIに聞く」の用途のうち、**「答える」部分は相談の中に戻す**。別の入口は要らない。

### 候補の数

- **F21**: 「**候補をいくつ出すか UI で選べるようにしたい**」。候補＝**相談が返すレシピ提案の数**（9/14 F13「候補 3 つ」の文脈で確認済み）。
  実装で裏取り: 相談は今、下書きを **1 件**だけ返す（`apps/server/src/lib/recipe-consult.ts`）。候補数という概念は無い。
- **F22**: 選ぶ場所は「**両方**」（設定で既定を決め、相談画面でその都度上書き）。

### §7-10 の未決 4 件

- **F23**: 入口 → 「**ホームに常設の入口を置く**」。
- **F24**: 家族の端末への表示 → 「**今と同じで何も出さない**」（手で足したときと同じ扱い）。
- **F25**: 取り消し → 「**必要。Toast でその場で戻せること**」（v1 の買い物リストでも）。
- **F26**: 掲載文 → 「**含めない。リリース時に別途**」。

### 献立の共有（割り込み）

- **F27**: 「**献立も家族と共有したいよね。カレンダーとかで。。**」
  実装で裏取り: **献立は同期対象外で家族に届いていない**（`sync-*.ts` に `menu_plan` 無し）。既存の `calendar.tsx` は**調理記録**だけを出す。
- **F28**: 位置づけ → 「**献立共有を設計・実装中に並行して検討を進める。リリースは同じ**」。
- **F29**: 「カレンダーとかで」の形 → 「**今はカレンダーというよりタイムライン。カレンダービューで見たい**」。

### 止める範囲

- **F30**: PR #337/#339/#338（タイムアウト修正）は「**含まれる。要件を反映してから作り直す**」。

## 詰まりの構造（第 1 部からの更新）

第 1 部の「#331 は §7 の前提」に加えて、**§7 自体にも本人未確認の箇所があった**（§7-4 の確認カード、§7-7 の入口、§7-3 の「説明する」の範囲）。
今回で §7-10 の 4 件は決まり、§7 の未確認箇所は本人確認が取れた。**設計書 §7 と今回の差分**は下の R に書く。

## 設計に渡せる要求 `R`（第 2 部）

| #       | 要求                                                                                                               | 根拠     | §7 との関係                                                                            |
| ------- | ------------------------------------------------------------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------- |
| **R10** | 操作は確認カードをタップしてから実行する。AI が勝手に実行する経路は作らない                                        | F16      | §7-4 そのまま（本人確認済み）                                                          |
| **R11** | v1 の操作は「買い物リストに足す」。次点は献立・作った記録・在庫減                                                  | F17・F18 | §7-2 の 9 操作から絞る                                                                 |
| **R12** | 相談の中で「どの画面でやるか」「その機能があるか」を答える                                                         | F19・F20 | **§7-3 の拡張**（手順の説明 → 場所と有無も答える）                                     |
| **R13** | 相談が返すレシピ提案の数を、設定の既定値＋相談画面での上書きで選べる                                               | F21・F22 | **§7 に無い。新規**                                                                    |
| **R14** | 相談への常設の入口をホームに置く                                                                                   | F23      | §7-7 の未決を決定                                                                      |
| **R15** | 相談経由の操作を家族の端末に特別に表示しない                                                                       | F24      | §7-6 の未決を決定                                                                      |
| **R16** | 実行直後の Toast に［取り消す］を置く（v1 の買い物リストから）                                                     | F25      | §7-4・§7-5。取り消し基盤の別 Epic を v1 に先行させるか、買い物だけの簡易版かは設計判断 |
| **R17** | 献立を家族と共有し、カレンダービューで見られること。エージェント化と**並行して設計・実装し、同じリリースに載せる** | F27〜F29 | **§7 に無い。別トラック・同一リリース**                                                |
| **R18** | PR #337/#339/#338 は R1〜R4・R10〜R16 を反映して作り直す                                                           | F30      | —                                                                                      |

## 要求されなかったこと（第 2 部）

- **言ったらそのまま実行する形**（確認カード無し）… F16 で「確認してから実行でよい」
- **料理そのものの質問に答えること**… F19 は場所と機能の有無。料理の手順・代用は挙がらなかった
- **使い方専用の入口・画面**… F20 で「相談の中で答えてくれればよい」
- **家族の端末への「相談で足された」印**… F24 で「何も出さない」
- **掲載文の方針を今決めること**… F26
- **OS のカレンダーへの書き出し**… F29 で選ばれなかった（アプリ内のカレンダービュー）
- **献立共有をエージェント化より先にやること**… F28 は「並行」

## 優先順位（第 2 部）

1. **R18**（作り直しの前提整理）… 何を作り直すかが決まらないと着手できない
2. **R10・R11・R16**（確認カード＋買い物リストに足す＋Toast 取り消し）… v1 の本体
3. **R14**（ホームの入口）… v1 と同時でないと使われない
4. **R12・R13**（使い方に答える・候補数）… 相談の返答形式の拡張。R1（写真の代わりに読み取り内容を送る）と同じ返答スキーマ変更に乗せる
5. **R17**（献立共有）… 並行トラック。同期の拡張なので `docs/クラウド同期設計.md` 側の設計が要る
6. R15 は「何もしない」なので実装項目なし

## まだ聞けていないこと（第 2 部）

- **R16 の実現方法**: 取り消し基盤（別 Epic・§7-5）を v1 に先行させるか、買い物リスト限定の簡易版か。**設計判断として app-leader へ**
- **R13 の候補数の既定値**（3？）と上限。聞いていない
- **R17 の中身**: カレンダービューに**献立だけ**か、**調理記録と並べる**か。「今はタイムライン」という評価は既存の記録画面についてのもので、献立との合流までは聞けていない
- **R17 の同期方式**: 献立を家族単位で 1 つにするのか、端末ごとの献立を見せ合うのか
- **R12 の答え方**: 相談が画面へ誘導（deep link）までするのか、文章で答えるだけか

## 設計書 §7 へ反映が要る差分（PR #330 のマージ前に）

- §7-3 に R12（場所と機能の有無を答える）を足す
- §7-6 を「何も出さない（決定）」に
- §7-7 を「ホームに常設（決定）」に
- §7-10 の 4 件を決定済みへ（掲載文だけ「リリース時に別途」）
- §7 に R13（候補数）と R17（献立共有・並行トラック）を追記。R17 は規模が違うので節を分ける

---

---

# 第 3 部: 献立の家族共有とカレンダービュー（同日・続き）

## きっかけ・方法・回答者

第 2 部の割り込み F27「献立も家族と共有したいよね。カレンダーとかで」を、利用者の指示
「**並行してここで、カレンダーの件を要件レベルで完璧に実現させる**」で掘った。
選択式 3 往復＋復唱 1 往復。回答者は本人（1 人）。**一般化に注意。**

音声入力の変換ずれが 1 か所あり、**読み替えを明示して確認した**（F35）。

## 分かったこと `F`

### 実績（仮定ではなく）

- **F31**: 直近で「今日（今週）何を作るか」を家族に伝えたのは「**口頭・LINE などのチャットで文字**」。アプリは使っていない。
  実装で裏取り: 献立は `sync-*.ts` に無く**家族に届かない**。`menu_plans` は時間帯ごと 1 本・`menu_plan_days.recipeId` は単数で
  **1 日 1 レシピ**（`apps/mobile/src/db/schema.ts:612-652`）。

### 献立の単位と編集

- **F32**: 「**家族で 1 つ。誰が変えても同じものが見える**」（買い物リストと同じ扱い）。
- **F33**: 変えられるのは「**家族全員**」。
- **F34**: 時間帯は「**設定で選べる**」（今は朝・昼・夕を別々に持てる）。

### 1 食の構成（読み替え確認済み）

- **F35**: 自由記述（音声入力）を次のように読み替えて「合っている」を得た:
  「週ビューについてだが、**各食一品ではない**。メイン料理は分かりつつ、置き場所は UX の専門家に任せたいが、
  **副菜や汁物・サラダも分かるようにしたい**。献立の一括作成は**主菜しか作らない**ので、副菜・サラダも含めた
  **1 週間分を作ってほしい**。データの持ち方を変え、既存機能とプロンプトを組み合わせればできる」。
  実装で裏取り: 一括生成のプロンプトは主菜前提（`apps/server/src/lib/menu-recipes.ts:122`）。
- **F36**: 1 食は「**主菜 1 品＋副菜・汁物・サラダは任意で複数**」を基本に、「**ある程度カスタマイズできるようにしたい。ユーザー設定的な**」。
- **F37**: カスタマイズの中身は 4 つとも: 「**枠の種類を選べる／枠の数を変えられる／時間帯ごとに構成を変えられる／一括生成でどの枠を埋めるかを選べる**」。
- **F38**: 1 週間分の副菜・汁物・サラダを作るのは「**両方**」（在庫・蔵書から組む／一括 AI 生成）。
- **F39**: 推論回数が増える件（1 週間 × 3〜4 品＝ 20〜28 品、上限 30 回/日）は「**上限を上げてよい**」。

### カレンダービュー

- **F40**: 中身は「**献立（予定）と調理記録（実績）を同じカレンダーに**」。
- **F41**: 幅は「**週（今週・来週）**」。
- **F42**: 作ったら「**予定が実績に変わる（自動でチェックが付く）**」。
  実装で裏取り: `menu-plan.service.ts:395-406` に、同じレシピの調理記録があれば `doneAt` を付ける仕組みが**既にある**（1 日 1 レシピ前提）。
- **F43**: 置き場所と見せ方は「**ペルソナ 6 人のレビュー＋ app-leader で案を出させ、最後は自分が選ぶ**」。

### 変更の通知

- **F44**: 家族が献立を変えたら「**変わったら常に通知**」。
- **F45**: 通知には「**誰が変えたかまで出す**」。
  → `createdBy` の同期とプライバシーポリシーの更新を伴うことを質問文で示した上での選択。
  **9/14 の F12 では「誰が変えたのか分かるようにしたい」は選ばれていなかった。今回で変更**（献立に限る）。
  実装で裏取り: 家族向け push は今、固定文「家族の共有データが更新されました」のみ（設計書 §7-6）。

## 詰まりの構造

```
献立は端末ローカル（F31）──▶ 家族に伝えるのは口頭・LINE（F31）──▶ アプリを開く理由が無い
        │
        └─▶ 1 日 1 レシピ（F31）──▶ 副菜・汁物が載らない（F35）──▶ 献立として使い物にならない
                                      └─▶ 一括生成も主菜だけ（F35）
```

**輪が切れているのは「家族に届かない」と「一品しか持てない」の 2 か所。** どちらもデータの持ち方の問題で、
本人も「データの持ち方を変えればできる」と見ている（F35）。

## 設計に渡せる要求 `R`（第 3 部）

| #       | 要求                                                                                                            | 根拠     |
| ------- | --------------------------------------------------------------------------------------------------------------- | -------- |
| **R19** | 献立を家族で 1 つにし、家族全員が編集できる。誰が変えても同じものが見える                                       | F32・F33 |
| **R20** | 1 食は主菜 1 品＋任意の枠（副菜・汁物・サラダ…）を複数持てる                                                    | F36      |
| **R21** | 枠の種類・枠の数・時間帯ごとの構成・一括生成で埋める枠を、ユーザー設定で変えられる                              | F37      |
| **R22** | 「在庫・蔵書から組む」と「一括 AI 生成」の両方が、R20 の構成で 1 週間分を出す                                   | F38      |
| **R23** | 一括 AI 生成の推論回数の上限は上げてよい                                                                        | F39      |
| **R24** | 献立（予定）と調理記録（実績）を同じカレンダーに、週の幅で見る                                                  | F40・F41 |
| **R25** | 献立の日に「作った」を記録したら、予定が実績に変わる（自動でチェック）。既存の `doneAt` を R20 の複数品に広げる | F42      |
| **R26** | 家族が献立を変えたら常に通知し、誰が変えたかまで出す（`createdBy` の同期・プライバシーポリシー更新を含む）      | F44・F45 |
| **R27** | 共有する時間帯は設定で選べる                                                                                    | F34      |
| **R28** | 週ビューの置き場所と見せ方は、ペルソナ 6 人＋ app-leader が案を出し、本人が選ぶ                                 | F43      |

R17（第 2 部）はこれで具体化された。**エージェント化と並行・同一リリース**（F28）。

## 要求されなかったこと（第 3 部）

- **各自の献立を見せ合う形**… F32 で「家族で 1 つ」
- **決める人 1 人・他は閲覧**… F33 で「家族全員」
- **献立だけの別カレンダー**… F40 で「同じカレンダーに」
- **月グリッド**… F41 で「週」（切り替えも選ばれなかった）
- **黙って更新／今日明日だけ通知**… F44 で「常に通知」
- **通知に「誰が」を出さないこと**… F45 で「出す」（9/14 からの変更）
- **役割を固定した枠（一汁三菜の固定 3 枠）／役割なしのリスト**… F36 で「主菜 1＋任意の枠」
- **推論回数を増やさない設計に縛ること**… F39 で「上限を上げてよい」

## 優先順位（第 3 部）

1. **R20・R21**（データの持ち方: 複数品＋枠設定）… 他の全部がこれに乗る。本人も「データの持ち方を変えれば」と指摘
2. **R19・R26**（家族で 1 つ＋通知＋createdBy）… 同期の拡張。`docs/クラウド同期設計.md` 側の設計と、プライバシーポリシー・データセーフティ申告の見直しを伴う
3. **R24・R25**（週カレンダー＋予定→実績）… 画面。R28 の案出しを先に
4. **R22・R23**（1 週間分の生成）… プロンプトと `INFER_GLOBAL_DAILY_LIMIT` の見直し
5. R27 は設定 1 つ

依存: R24 は R20 が無いと副菜が出せない。R26 は R19 が無いと通知する相手がいない。

## まだ聞けていないこと（第 3 部）

- **週の始まり**（月曜／日曜）と、**来週まで**で足りるか
- **通知の頻度**: 連続して変えたときにまとめるか、1 変更 1 通知か
- **一括 AI 生成の 1 回あたりの品数**と、1 推論で複数品をまとめるか（設計側の案待ち）
- **既存の献立データの移行**（1 日 1 レシピ → 複数品）。作り直しでよいか
- **`createdBy` を出す範囲**: 献立だけか、買い物・在庫にも広げるか（第 2 部 R15 は「相談経由の操作は何も出さない」。**献立の通知とは別の話として整理が要る**）
- **プライバシーポリシーのどの節を変えるか**（`docs/privacy-policy.md`）

## 設計書へ反映が要る差分

- `docs/相談してレシピを作る設計.md` §7 の「別トラック」節 → 本節を参照
- `docs/クラウド同期設計.md`: `menu_plans` / `menu_plan_days` / `createdBy` の同期（新規）
- `docs/買い物リスト・在庫設計.md` か新規の献立設計: R20〜R22 のデータモデル
- `docs/privacy-policy.md`: 「誰が変えたか」を家族に見せる旨（R26）

## 決定（2026-09-16・ペルソナ評価の後・本人）

評価: [persona-week-view-2026-09-16.md](persona-week-view-2026-09-16.md)（案 2 ×3・案 1 ×2・案 3 ×1）。**本人の選択は多数と違う**。そのまま記録する。

| #   | 決定                                                                                         | 本人の言葉                                         | 変わる要求                                                                                                                                                               |
| --- | -------------------------------------------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D1  | 週ビューは**案 1**（献立の面を縦 7 行の週ビューに。時間帯はチップ切替。カレンダーは 1 つに） | 「案 1: 献立の面を縦 7 行の週ビューに」            | R28 → 決定                                                                                                                                                               |
| D2  | **「誰が変えたか」は後回し**。通知は買い物・在庫と同じ枠                                     | 「『誰が』は後回し。通知は買い物・在庫と同じ枠で」 | **R26 を差し替え**: 献立の変更は既存の家族 push（固定文・グループ単位のデバウンス）に乗せる。`createdBy` の同期・§0-2 の撤回・プライバシーポリシー更新は**今回やらない** |
| D3  | **調理記録も同期する**（家族の ✓ が届く）                                                    | 「調理記録も同期する（家族の ✓ が届く）」          | **R29（新規）**: `cooking_logs` を同期対象に足す。写真を含むかは未決                                                                                                     |

### D1 に付く、どの案でも直すこと（ペルソナ複数）

- 記号だけの表示をやめて言葉で（「済み」「予定」）。12px を使わない（のりこ）
- 「by Papa」は実名に（エマ）。ただし D2 で「誰が」自体が後回しなので、v1 では出ない
- 案 1 固有: 長い料理名は `numberOfLines={1}`＋省略、「＋Add」は半角に揃える、時間帯チップの切替を忘れる問題（ひな・のりこ）への手当は設計側で

### 未決（D 後に残るもの）

- 週の始まり／来週まで／既存献立データの移行（app-leader §6 の 1・2・4）
- **R29 の範囲**: 調理記録の写真を同期に含めるか（含めると写真の R2 経路に乗る）。`userName` は同期しないので「誰が作ったか」も v1 では出ない（D2 と整合）
- 同期版 3→4（全端末が一度全量取り直し）の受け入れ。献立と調理記録を**同じ版上げで 1 回にまとめる**のが自然（設計側の案）

---

---

# 第 4 部: カレンダーのウィジェット（同日・続き）

## きっかけ・方法・回答者

利用者「**カレンダーについては、ウィジェットも対象です。固まったものは受付表にする。**」
選択式 2 往復＋復唱 1 往復。回答者は本人（1 人）。**一般化に注意。**

## 分かったこと `F`

### 今の実装（聞く前に確認）

- Android の献立ウィジェット（W2）は**既にある**。小/中＝「今日の一品」（1 レシピ名）、大（高さ 250dp 以上）＝週間 7 行（1 日 1 レシピ・作った日は薄く）。タップでレシピ詳細か献立画面（`docs/ウィジェット設計.md` §10、`apps/mobile/src/widgets/menuWidgetContent.ts:40-42`）
- iOS は買い物ウィジェットのみ。献立は読まない（`targets/shopping-widget/ShoppingWidget.swift:39`）
- スナップショットは**アプリが動いたときにしか書かれない**（`widget-snapshot.service.ts:177` `writeWidgetSnapshotNow`）。Android の再描画は 30 分下限
- **アプリ内のどこにもウィジェットの案内が無い**（設定・オンボーディング・ヘルプの i18n に一致なし）

### 本人の言葉

- **F46**: 献立ウィジェットは「**置いていない**」。理由は「**あることを知らなかった・忘れていた**」（中身の不満ではない）
- **F47**: 出すものは「**今日の主菜＋副菜・汁物まで**」「**週 7 日の主菜だけ**」「**週 7 日の主菜＋副菜まで**」の 3 つを「**ウィジェットの大きさで分ける**」
- **F48**: タップ先は「**タップした日のレシピ**」（今の挙動のまま）
- **F49**: 更新は「**家族の変更が push で届いたら自動で新しく**」。対象は「**献立も買い物も**」
- **F50**: 「**iOS も同じ内容で出す**」

## 詰まりの構造

既存の W2 は「1 日 1 品・アプリを開いたときだけ更新・Android のみ・案内なし」。第 3 部で献立が家族共有＋複数品になると、この 4 つすべてが古くなる。本人が置いていない理由は「知らなかった」なので、**中身を直しても案内が無ければ置かれない**（案内は要求されていない → 「まだ聞けていないこと」へ）。

## 設計に渡せる要求 `R`（第 4 部）

| #       | 要求                                                                                                                 | 根拠 |
| ------- | -------------------------------------------------------------------------------------------------------------------- | ---- |
| **R30** | ウィジェットの大きさで出し分ける: 小/中＝今日の主菜＋副菜・汁物、大＝週 7 日の主菜、それ以上＝週 7 日の主菜＋副菜    | F47  |
| **R31** | タップ先はタップした日のレシピ（現状維持）                                                                           | F48  |
| **R32** | 家族の変更が push で届いたら、献立・買い物の両ウィジェットを自動で新しくする（背景同期＋スナップショット再書き出し） | F49  |
| **R33** | iOS にも献立ウィジェットを同じ内容で出す（Swift 側の新規実装）                                                       | F50  |

R30 は R20（複数品）と R19（家族共有）に依存。R32 は同期の push 受信を背景で処理する仕組み（今は無い）に依存し、**iOS の更新予算（40〜70 回/日・`docs/ウィジェット設計.md` §2）に当たる**。

## 要求されなかったこと（第 4 部）

- **ウィジェット内の切替（週／今日）**… 大きさで分ける（F47）。設計書 §10 の「ウィジェット内トグルは使わない」とも整合
- **タップで週ビューを開くこと**… F48 で「その日のレシピ」
- **献立だけ自動更新**… F49 で「献立も買い物も」
- **Android 先行・iOS 後**… F50 で「同じ内容で」
- **ウィジェットの存在を案内すること**… 聞いていない（F46 の理由から必要そうだが、**要求されていないので R にしない**）

## 優先順位（第 4 部）

1. **R30**（出し分け）… R20/R19 の後。データが揃えば `menuWidgetContent.ts` の拡張
2. **R33**（iOS）… W2 を Swift で新規。工数は別枠（`docs/ウィジェット設計.md` §3・§8）
3. **R32**（push 自動更新）… 背景同期の新設。電池・iOS 予算の検証が要る。単独で切り出せる
4. R31 は変更なし

## まだ聞けていないこと（第 4 部）

- **ウィジェットの案内**（設定やオンボーディングで存在を知らせるか）。F46 の原因そのものだが要求されていない
- **「それ以上」の大きさの閾値**（週の主菜＋副菜を出す高さ）
- **R32 の頻度上限**（iOS は 1 日の更新回数に上限。家族が細かく変えると届かない回が出る）
- **時間帯**（R27 で設定可）をウィジェットにどう反映するか。今は夕優先・朝/昼は見出しに付けるだけ

## 決定（2026-09-16・受付票の提示時・本人）

| #   | 決定                                                         | 本人の言葉                                       |
| --- | ------------------------------------------------------------ | ------------------------------------------------ |
| D4  | 取り消しは買い物限定の簡易版で先に出す（基盤は後で差し替え） | 「R16: 取り消しは買い物限定の簡易版で先に出す」  |
| D5  | 候補数の既定は 3、上限 5                                     | 「R13: 候補数の既定は 3、上限 5」                |
| D6  | 週の始まりは日曜のまま                                       | 「週の始まりは日曜のまま（既存と揃える）」       |
| D7  | 使い方への答えは v1 は文章のみ（deep link は後）             | 「R12: v1 は文章で答えるだけ（deep link は後）」 |

受付票: `docs/plans/2026-09-16-consult-agent-and-menu-sharing.md`（合意は本人が読んでから）。

---

---

# 第 5 部: 一括生成の非同期化（app-leader 設計レビュー中に発覚・翌日 2026-09-17）

## きっかけ・方法・回答者

app-leader の献立トラック設計（一括生成の分割方式・§5-3）を提示したところ、
「バックグラウンドでパラレル。終わったらプッシュ通知とかにしないとかなり使いづらい気がする」という
割り込みが入った。選択式3往復。回答者は本人（1人）。**一般化に注意**。

## 分かったこと `F`

- **F51**: 一括生成（主菜のみ・7日分）を「**まだ使ったことがない**」。待ち時間で困った実体験は無い。
  懸念は使う前の予測
- **F52**: 適用範囲は「**主菜のみの場合も含めて全部バックグラウンド化**」（今回の枠付き3分割だけでなく、既存の一括生成も対象）
- **F53**: 待っている間「**アプリを閉じて他のことをしてもよい**」「**push通知必須**」

実装で裏取り: 現在の一括生成は**完全に同期**（`menu.tsx:158` `await generateMenuPlan(...)` →
`ActivityIndicator`）。サーバーへの非同期ジョブ基盤・完了時のpush送信コードは**どちらも存在しない**
（Expo Pushの送信自体は同期系の`notifyGroupDevices`で使われているが、用途が違う）。

## 詰まりの構造

これは既存の詰まり（相談のタイムアウト・献立が届かない）とは別系統。**実際に使って困った話ではなく、
枠が増えて推論が3回に分かれると聞いて先回りで心配した話**。ただし、待つ間アプリを閉じてよい・push必須、
という要求自体は具体的で、実装すると次の新規基盤が要る:

1. サーバー側の非同期ジョブ実行（現在はリクエスト内で同期的にGemini呼び出し→レスポンス）
2. ジョブ完了時のpush通知送信（新規。既存のExpo Push基盤は同期トリガー専用）
3. クライアント側: ジョブ投入→受理→(アプリを閉じても)完了を待つ→push受信→結果表示、の導線

## 設計に渡せる要求（保留・要スコープ判断）

- **R34（保留）**: 一括生成（主菜のみ・枠付き3分割とも）をバックグラウンドジョブ化し、完了をpush通知する
  - 根拠: F52・F53
  - **規模が違う**: サーバーに非同期ジョブ基盤が無い状態からの新設。献立トラック本体（PR-1〜PR-7・15〜19日）とは独立した基盤追加
  - 実体験に基づく要求ではない（F51）ため、優先度は**利用者に確認が要る**

## まだ聞けていないこと

- R34を今回のリリースに含めるか、実際に一括生成を使ってみてから判断するか
- ジョブが失敗した場合の扱い（push で「失敗しました」を出すか、黙って諦めるか）
- 複数の一括生成を連続で頼んだときの扱い（キューに積むか、前のジョブを上書きするか）
