# 週ビュー設計案（app-leader・Opus・2026-09-16）— 3 案

> 出所: app-leader が実装を読んで書いた案。file:line は実コード。`audit-design-drift` も併走。**決定はしない。**

## 0. ブリーフ「今の実装（事実）」の訂正 3 件

| ブリーフの記述                                               | 実物                                                                                | 根拠                                                             |
| ------------------------------------------------------------ | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| 「ボトムタブ: ホーム／レシピ／追加／在庫／買物／設定」       | タブは **5 本**。**設定はタブではない**（ホームのヘッダの歯車）                     | `apps/mobile/app/(tabs)/_layout.tsx:55-112`、`index.tsx:346-356` |
| 「献立は `href:null` ではなく `(tabs)/menu.tsx` として存在」 | **`href: null` である**。タブバーに献立は出ていない。主入口は**ホームの献立カード** | `_layout.tsx:120`、`index.tsx:411-436`                           |
| 「`src/theme.ts` の `Typography`」                           | 実体は `apps/mobile/src/constants/theme.ts:47-64`                                   | —                                                                |

影響: 「献立タブを週ビューにする」は、利用者から見れば**ホームのカードから開く 1 枚を作り替える話**。

## 1. 3 案に共通する土台（R19〜R23 はデータ側を変えないと成立しない）

### 1-1. 事実

| #   | 事実                                                                                            | 根拠                                                          |
| --- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| F1  | 献立は 1 日 1 レシピ。PK `(planId, day)`、`recipeId` 単数                                       | `schema.ts:647-666`                                           |
| F2  | 時間帯ごとに 1 本。`meal_time` UNIQUE                                                           | `schema.ts:622-625`                                           |
| F3  | 献立は同期対象外。同期種別 8 つに menu 系無し                                                   | `schema.ts:620`、`sync-payload.ts:57-76`                      |
| F4  | 献立の日は**日付を持たない**。`anchorDate` は「自動モード かつ 夕」のときだけ                   | `menu-plan.service.ts:322`、`schema.ts:630-631`               |
| F5  | 予定→実績は `recipeId` 一致＋`cookedAt >= generatedAt`。**日付は見ていない**                    | `menu-plan.service.ts:377-392`                                |
| F6  | 家族通知は**サーバー発の固定文のみ**（名前・件数・種別なし）                                    | `server/routes/sync.ts:370-377`                               |
| F7  | 通知はグループ単位でデバウンス。通常 5 分／買い物・在庫を含むとき 60 秒                         | `sync.ts:386-401, 413-420`                                    |
| F8  | サーバーは端末の表示名を持たない（列を DROP 済み）                                              | `sync-store.ts:49-51`                                         |
| F9  | ローカル `created_by` は**全端末で `user-kei` 固定**                                            | `user.service.ts:19`、`sync-payload.ts:13-14, 194`            |
| F10 | pull は `updatedByDevice`（端末 ID）を運んでいる                                                | `sync-store.ts:610-613`、`sync-runner.service.ts:489`         |
| F11 | 同期種別を増やすと `SYNC_PAYLOAD_SCHEMA_VERSION` を上げる必要。**全端末が一度だけ全量取り直し** | `sync-payload.ts:24-34`                                       |
| F12 | `RECIPES_SCOPE_TYPES` はサーバーの同名定数と一致必須                                            | `sync-payload.ts:618-628`                                     |
| F13 | カレンダーは**日曜始まり固定**。マトリクスと曜日ラベルが別々にハードコード                      | `utils/calendar.ts:25, 29-30`、`ja/ui.ts:133`、`en/ui.ts:108` |
| F14 | カレンダー画面は調理記録だけを月グリッドで                                                      | `calendar.tsx:1-3, 17, 38, 48-49`                             |
| F15 | 調理記録も同期対象外。`userName` はローカル                                                     | `sync-payload.ts:67-76`、`timeline.service.ts:27`             |
| F16 | 献立はバックアップ対象                                                                          | `backup.service.ts:283-302`                                   |
| F17 | 一括生成プロンプトは主菜前提・枠の概念無し                                                      | `menu-recipes.ts:109-141`                                     |
| F18 | AI 上限は env `INFER_GLOBAL_DAILY_LIMIT`。コード変更なしで上げられる                            | —                                                             |

### 1-2. 設計書との整合（`audit-design-drift`）

- 画面一覧表（`画面設計.md:73-96`）は S19 止まり。実装は S20（menu）/S21（menu-settings）。**表が古い**。新画面は S22 以降
- カレンダーには **S 番号が無い**
- 献立の同期: 設計・スキーマ・実装は整合（ローカル専用）。ずれは「本日の R19」と「未転記の設計書」の間
- 「誰が変えたか」: `クラウド同期設計.md:83-86`「v1 では持たない（2026-08-21 決定 — **端末側生成なら適法だが**そもそも持たない方が明快）」。**R26 がこれを献立に限り上書き**。設計書自身が「端末側生成なら適法」と書いている
- 1 日 1 品: `買い物リスト・在庫設計.md:566`「行分割は **#283**」。R20〜R22 は #283 の具体化

### 1-3. どの案でも必要な変更

- **(a) 枠**: `menu_plan_days` PK を `(plan_id, day, slot_id)` へ。`slot_kind`（`main`｜任意）/`position`。枠定義は新テーブル（例 `menu_slot_settings`）。主菜 1 品は `slot_kind='main'` が 1 本という形でしか表せない
- **(b) 家族で 1 つ**: `SYNC_ENTITY_MENU_PLAN` 追加、**版 3→4**（全端末が一度全量取り直し）、`RECIPES_SCOPE_TYPES` はサーバーと同時変更、`meal_time` UNIQUE は**自然キー受信**（`NATURAL_KEY_ENTITY_TYPES` と同型。S2 の辞書系で踏んだ罠）
- **(c) 日付**: `menu_plan_days` に `date` を持たせる。`day` 連番を残すかは `rollMenuPlan`/`toRollableDays`/`fillMenuPlanShortfall` 全経路に効く（未決）。日付が入ると F5 の突合を「同じ日」に締められるが既存挙動が変わる
- **(d) 誰が変えたか**: §0-2 が禁じるのは「サーバーが文面に名前を入れて送る」こと。経路は (1) payload に表示名を載せる（サーバーに表示名が載る → ポリシー要確認）(2) `updatedByDevice` を使い端末 ID→名前をローカルで対応付け（サーバーは名前を持たない）(3) 受信した端末が**自分でローカル通知**（`notification.service.ts` の基盤あり）。サーバーの固定文 push は触らない
- **(e) 通知頻度**: デバウンスはグループ単位 1 スロット。献立を急ぎにすると買い物・在庫と食い合う。別スロットなら `lastNotifiedAt` の鍵を種別クラスへ
- **(f) 一括生成**: 枠構成を渡し枠ごとにラベル付きで返す。`GEMINI_RESPONSE_SCHEMA` に `slotKind`。サーバーが正典・モバイルが写し（両方直す）
- **(g) R23**: env のみ。1 回の出力長が効く

---

## 2. 案 1 — 献立の面を週ビューに作り替える（縦 7 行・時間帯はチップで切替）

### 置き場所

`menu.tsx`（S20）を週ビューへ置き換え（href:null のまま）。日数チップ・`DayCard` を置き換え、歯車・不足バナー・一括生成・買い物追加・時間帯チップは残す。ホームの献立カードは残す。**ホームの「カレンダー」ボタンの遷移先を週ビューへ変え**、月表示（`calendar.tsx`）は週ビュー内「月で見る」から開く従属画面へ降ろす（カレンダーを 1 つにするため）。

### 1 日のセル（328px・主菜 15px medium・副菜 13px）

```
日本語
┌────────────────────────────────────────┐
│ 9/16 火                        ＋ 枠追加│ 12px
│ ● 鶏の照り焼き                    40分  │ 15px
│ ・ ほうれん草のおひたし                 │ 13px
│ ・ わかめの味噌汁                       │
└────────────────────────────────────────┘
English
┌────────────────────────────────────────┐
│ Tue 9/16                        ＋ Add  │
│ ● Teriyaki chicken               40 min │
│ · Spinach with sesame                   │
│ · Miso soup with wakame                 │
└────────────────────────────────────────┘
```

英語で切れる箇所: 主菜行は 268px（15px で半角約 30 字）。`Slow-cooked pork belly with daikon`（34 字）は切れる → `numberOfLines={1}`。「＋ Add」は 6 半角以内。**7 日 ≈ 672px で 1 画面（可視約 560px）に収まらない**（5 日見えて 2 日スクロール）。

### 予定→実績

左に細いゴールド縦罫。作った枠は `●`→`✓`、料理名を `paperDim`、行末にアバター。既存 `cardDone: opacity 0.5` は使わない（老眼で読めない）。献立に無い調理記録は `＋ 予定外: 麻婆豆腐` を 1 行足す。

### 家族の変更

変わった枠の右に「健が変更」（12px）。既読で消す。通知は §1-3(d)(3) の端末側ローカル通知。**開いているときは黙って再読込ではなく「健が 9/18 の夕を変えました［見る］」バー**が要る（買い物・在庫の現行と違う扱い）。

### 時間帯設定

時間帯チップをそのまま。1 画面 = 1 時間帯。オフの時間帯はチップごと出さない。欠点: 朝昼夕が同時に見えない。

### 触るファイル

新規 3（`MenuWeekRow.tsx`・`utils/menuWeek.ts`（純関数）・`sync-menu-entities.service.ts`）／変更 18（`menu.tsx` 全面・`index.tsx:327-334`・`calendar.tsx`・`menu-settings.tsx`・schema/migrate/menuPlanStorage/menuPlan/menu-plan.service/sync-payload/sync-entities/sync-runner/backup/menu-recipes.provider/server menu-recipes/sync-store/sync routes・i18n）。触らない: `utils/calendar.ts`。

### 利点・欠点

利点: (1) 料理名が読める唯一の形（328px 全幅・13px を割らない）(2) 既存 menu の周辺機能をそのまま載せ替え (3) カレンダーが 1 つになる。
欠点: (1) **7 日が 1 画面に収まらない** (2) 時間帯が 1 画面 1 つ (3) ホームの「カレンダー」の行き先が変わり既存の慣れを壊す。

| R19       | R20    | R21    | R22       | R23    | R24       | R25    | R26       | R27    |
| --------- | ------ | ------ | --------- | ------ | --------- | ------ | --------- | ------ |
| 条件付\*1 | 満たす | 満たす | 条件付\*2 | 満たす | 条件付\*3 | 満たす | 条件付\*4 | 満たす |

*1 自然キー受信 *2 `menu-recipes.ts` 枠対応 *3 1 画面に収まらない *4 端末側生成

### 未決への依存

週の始まり: 影響小。来週まで: `‹ ›` の枚数で `rollMenuPlan` の扱いが変わる。既存データ移行: 日付が無い（F4）ので今週のどこへ置くか未決。

---

## 3. 案 2 — カレンダー画面に週／月を足す（7 列グリッド＋選択日の詳細）

### 置き場所

`calendar.tsx`（**S22 を振る**）に「週｜月」切替。週では献立と調理記録を同じグリッドに。置き換えるもの: なし。`menu.tsx` は**編集面として残る**（「見るのはカレンダー・組むのは献立」）。ホームは 1 文字も変わらない。

### 1 日のセル（360÷7 = 46.8px → グリッドは印だけ、中身は選択日の詳細）

```
日本語
┌────────────────────────────────────────┐
│ ‹        9/15 – 9/21              ›    │
│  週  │  月                              │
│  月   火   水   木   金   土   日       │ 12px / 46.8px 列
│  15   16   17   18   19   20   21      │
│  ◍    ◍    ○    ○    ○    ·    ·       │ 予定の充填度
│  ✓✓   ✓                                │ 実績の数
└────────────────────────────────────────┘
┌────────────────────────────────────────┐
│ 9/16 火          予定 3品 / 作った 1品  │
│ ● 鶏の照り焼き                  ✓ 健    │
│ ・ ほうれん草のおひたし                 │
│ ・ わかめの味噌汁                       │
└────────────────────────────────────────┘
English
┌────────────────────────────────────────┐
│ ‹        Sep 15 – Sep 21          ›    │
│  Week │  Month                          │
│  Mon  Tue  Wed  Thu  Fri  Sat  Sun     │
│   15   16   17   18   19   20   21     │
│   ◍    ◍    ○    ○    ○    ·    ·      │
│   ✓✓   ✓                               │
└────────────────────────────────────────┘
┌────────────────────────────────────────┐
│ Tue Sep 16        3 planned / 1 cooked │
│ ● Teriyaki chicken              ✓ Ken  │
│ · Spinach with sesame                  │
│ · Miso soup with wakame                │
└────────────────────────────────────────┘
```

英語で切れる箇所: 曜日 3 字略は収まる。**`3 planned / 1 cooked` が危ない**（複数形で `12 planned / 10 cooked` まで伸びる。日本語の 1.2 倍）。**`Week | Month`（約 80px）は日本語の 2.4 倍**。週範囲と同じ行に置くと崩れる → 2 行に分ける必要。

### 予定→実績

グリッド 2 段（上: 予定の充填度 `◍/○/·`、下: `✓` の数）。**予定と実績の食い違いが一目で分かる**（○ なのに ✓ が無い日が続く＝献立の形骸化）— この形だけの利点。

### 家族の変更

グリッド: 変更のあった日のセルに細い下線。詳細: 「健が変更」。**問題: 変更の見え方をカレンダー（S22）と献立（S20）の 2 画面に置く**ことになる。

### 時間帯設定

セル 46.8px に時間帯は埋められない。(i) 週グリッド上に時間帯チップ（1 時間帯ぶん）(ii) セルに 3 ドット（8px・**老眼で判別不能**）。(i) 前提。

### 触るファイル

新規 3（`WeekStrip.tsx`・`utils/menuWeek.ts`・`sync-menu-entities.service.ts`）／変更 16（`calendar.tsx`・`utils/calendar.ts`（`buildWeekRow` 追加）・`menu-settings.tsx`・共通 13・i18n menu+ui）。**`menu.tsx`（S20）は 1 行も触らない**。ホームも触らない。

### 利点・欠点

利点: (1) **7 日が確実に 1 画面**（グリッド約 110px）— R24 に最も忠実 (2) `calendar.tsx` の構造を週へ複製・`menu.tsx` 無傷＝回帰リスク最小 (3) 予定と実績の食い違いを可視化。
欠点: (1) **1 日の中身は常に 1 タップ先** (2) 見る／組むの 2 画面化（`audit-nav-duplication` 対象） (3) **英語ヘッダが最も危ない**。

| R19    | R20                            | R21    | R22    | R23    | R24        | R25    | R26                    | R27                  |
| ------ | ------------------------------ | ------ | ------ | ------ | ---------- | ------ | ---------------------- | -------------------- |
| 条件付 | 満たす（詳細を開いたときだけ） | 満たす | 条件付 | 満たす | **満たす** | 満たす | 条件付（2 画面に置く） | 条件付（チップ併用） |

### 未決への依存

**週の始まりに最も強く依存**（列順そのもの。F13 の 2 箇所を同時に回す）。既存データ移行: 日付の無いプランは週グリッドに出せない → 組み直させる判断が現実的。`createdBy`: 調理記録の `userName` がローカルのみ（F15）なのが目立つ。

---

## 4. 案 3 — 1 日 1 カード・時間帯を段で積む（縦スクロール）

### 置き場所

`menu.tsx`（S20）を週ビューへ。時間帯を切り替えず 1 日の中に積む（時間帯チップが消える）。ホームの献立カードを「今日→明日の 2 日ミニ帯」へ**置き換え**。**`calendar.tsx` は触らない**（月・実績のまま）→ **カレンダーが 2 つ残る**（最大の欠点）。

### 1 日のセル

```
日本語
┌────────────────────────────────────────┐
│ 9/16 火                                │
│ 朝  ● トーストと目玉焼き               │
│ 昼  ● 焼きうどん                       │
│ 夕  ● 鶏の照り焼き                ✓ 健 │
│     ・ ほうれん草のおひたし             │
│     ・ わかめの味噌汁                   │
└────────────────────────────────────────┘
English
┌────────────────────────────────────────┐
│ Tue 9/16                               │
│ Breakfast                              │ ← 独立行にせざるを得ない
│   ● Toast and fried egg                │
│ Lunch                                  │
│   ● Yaki udon                          │
│ Dinner                             ✓Ken│
│   ● Teriyaki chicken                   │
│   · Spinach with sesame                │
│   · Miso soup with wakame              │
└────────────────────────────────────────┘
```

**英語で最も深刻**: 「朝/昼/夕」は 1 文字≈20px だが `Breakfast` は約 63px。頭文字 `B/L/D` は不可（可読性）。英語だけ段ラベルを独立行にする → **日本語 6 行／英語 10 行 = 1.6 倍**。縦: 日 980px・英 1,500px。

### 予定→実績

段ごとに `✓＋作った人`。段全体が済んだら段ラベル左にゴールドの点。**粒度は最細**。

### 家族の変更

段の右に「健が変更」。英語 `Changed by Ken`（約 90px）は料理名の下に折る。

### 時間帯設定

**R27 と最も直結**（S21 の 3 スイッチ＝段の有無）。切替ゼロ。3 つオンで縦がさらに伸びる。

### 触るファイル

新規 3（`MenuDayCard.tsx`・`utils/menuWeek.ts`・`sync-menu-entities.service.ts`）／変更 17（`menu.tsx` 全面・`index.tsx:416-436`・`menu-settings.tsx`・共通 13・i18n menu）。触らない: `calendar.tsx`・`utils/calendar.ts`。

### 利点・欠点

利点: (1) 時間帯の切替ゼロ (2) 設定と画面の対応が説明不要 (3) `calendar.tsx` 無傷。
欠点: (1) **日英で縦 1.6 倍**（構造的） (2) **縦 980〜1,500px** で「見渡す」感覚が最も薄い (3) **カレンダーが 2 つ残る**（R24 の解釈が割れる）。

| R19    | R20    | R21    | R22    | R23    | R24                          | R25    | R26                        | R27        |
| ------ | ------ | ------ | ------ | ------ | ---------------------------- | ------ | -------------------------- | ---------- |
| 条件付 | 満たす | 満たす | 条件付 | 満たす | 条件付（月の実績が別に残る） | 満たす | 条件付（英語で行が折れる） | **満たす** |

### 未決への依存

来週まで: 縦が長いので今週＋来週の連続スクロールは 2,000px 超 → 「来週は別画面」に寄る。既存データ移行: 「朝が空の段」が 7 日並ぶ → 空段を出すか畳むか。

---

## 5. 比較表

|                                   | 案 1 縦 7 行・チップ     | 案 2 カレンダーに週/月 | 案 3 1 日カード・段 |
| --------------------------------- | ------------------------ | ---------------------- | ------------------- |
| 固有変更ファイル                  | 5                        | **3**                  | 4                   |
| `menu.tsx`                        | 全面                     | **触らない**           | 全面                |
| `calendar.tsx`                    | 従属へ降格               | **主戦場**             | 触らない            |
| ホーム                            | カレンダーボタンの遷移先 | **触らない**           | 献立カード置換      |
| `utils/calendar.ts`（日曜始まり） | 触らない                 | **触る**               | 触らない            |
| 新 S 番号                         | 不要                     | **S22**                | 不要                |
| 週が 1 画面に                     | ✕（672/560）             | **○**（110）           | ✕（980〜1,500）     |
| 料理名がそのまま読める            | **○**                    | △（詳細で ○）          | **○**               |
| 日英の縦の差                      | ほぼ無し                 | ほぼ無し（横幅が危険） | **1.6 倍**          |
| カレンダーが 1 つ                 | ○（降格）                | ○                      | ✕                   |
| 回帰リスク                        | 中                       | **低**                 | 中                  |
| `audit-nav-duplication` の懸念    | ホームの行き先           | 見る/組むの 2 画面     | カレンダー 2 枚     |

## 6. 未決（選ばない）

1. 週の始まり: (a) 日曜のまま (b) 月曜へ（2 箇所同時） (c) S21 で選べる
2. 来週まで: (a) 今週・来週 2 枚固定 (b) 前後無制限 (c) 今週のみ
3. 通知頻度: (a) 通常 5 分 (b) 急ぎ 60 秒（買い物と食い合う） (c) 独立スロット（通知疲れ・#78 と同型）
4. 既存データ移行: (a) `main` として読み連番で今週へ (b) 日付の無いプランは出さず組み直しを促す (c) 空にして組み直し（`clearMenuPlan` 既存）。`day` 連番を残すか `date` へ置き換えるか
5. `createdBy`: (a) payload に表示名（ポリシー要確認） (b) 端末 ID→名前をローカル対応付け (c) 献立だけ (d) 調理記録にも

## 7. 利用者の判断が要ること

1. 案 1/2/3 2. R26 の実現経路（ポリシー・データセーフティに波及） 3. `SYNC_PAYLOAD_SCHEMA_VERSION` 3→4 の受け入れ（全端末が一度全量取り直し） 4. §6 の 5 件 5. 設計書の先行更新（画面一覧表・カレンダーの S 番号・§0-2 の決定変更）

## 8. 提案（未確認）

- `docs-map.mjs` に `sync-` → クラウド同期設計 の規則が無い（更新督促が飛ばない）
- 判断を `utils/menuWeek.ts`（純関数）へ寄せる（`品質基準.md` §2.3）
- test-writer 向け変異候補: 主菜 2 本を弾く／自然キー受信で一意制約に落ちない／日付無しの旧プランで例外なし／英語ロケールの行数
