# レビュー記録: PR #346（トラックA・相談の送信）Gemini レビュー（2026-09-17）

- 状態: 完了
- 重大度: 高1件・中1件・軽微2件・外れ3件
- 指摘元: `mcp__plugin_harness_gemini__ask_gemini`（gemini-3.1-pro-high・検索0回）。材料: 受入基準A・R1品質評価の結論・PR #346全文差分
- 調査・判断・対応:

| #   | 指摘                                                                    | 判定               | 根拠                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | ----------------------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | BYOK経路にタイマー順序の修正が含まれていない                            | **外れ**           | `consultViaByok`（provider.ts:388-389）は元から `toWireMessages` の直後（:411）にタイマーを開始しており、要件を最初から満たしていた。修正が不要だったので差分に含まれていないだけ。ただし「BYOK経路のタイマー順序を固定するテストが無い」は事実（副次的に軽微な当たり）                                                                                                                                 |
| 2   | BYOK経路で `images` と `imageReadings` が二重送信される                 | **外れ**           | `consultViaByok` は `toWireMessages` を経由している（:389）。`toWireMessages` 内でimageReadingsがあれば `images` フィールド自体を含めないため、二重送信は起きない。Geminiは「BYOK経路がtoWireMessagesを経由しない」と誤解していた                                                                                                                                                                       |
| 3   | 予算切り捨て分岐がimageReadingsチェックより先にあり意図が曖昧           | **外れ**           | 実コードのコメント「写真が無い or 予算で落とされたメッセージ — imageReadings があっても添える」のとおり、この分岐でもimageReadingsは正しく引き継がれる。Gemini自身も「現行ロジックでも結果的に送られます」と認めており実害なし                                                                                                                                                                          |
| 4a  | `MAX_CONSULT_IMAGES_PER_MESSAGE===4` の直接テストは定数をなぞっただけ   | **当たり（軽微）** | 実際にUIで4枚添付できるか・5枚目が弾かれるかを検証するテストが無い。`consult.tsx` のUIテストが今回のスコープに無い                                                                                                                                                                                                                                                                                      |
| 4b  | 空imageReadingsのテストがプロバイダ自体をモックしバイパスしている       | **当たり（中）**   | `consult.route.test.ts` は全テストが `setConsultProviderForTesting` でスタブしており、`GeminiRecipeConsultProvider` の実際の `JSON.parse` → `.map().filter()` ロジックを直接検証するテストが0件（grep確認）                                                                                                                                                                                             |
| 5   | サーバーが `originalIndex` を破棄し `string[]` にダウングレードしている | **当たり（高）**   | `recipe-consult.ts:407-410` で確認。`imageReadings?.map((r) => r.reading?.trim())` は配列順そのままで、モデルが一部の写真だけ読み取り結果を返した場合（例: 2枚中1枚のみ `originalIndex:1` で返る）、クライアント側の `message.imageReadings.map((r,i) => \`写真${i+1}: ${r}\`)`（provider.ts:396付近）が配列インデックスをそのまま写真番号として使うため、誤ったラベル付けで2往復目以降の会話が食い違う |

- 対応:
  - #5（originalIndexの破棄）: **修正済み**（コミット `09f1914`）。`originalIndex` 昇順にソートしてから文字列化。順序のズレは解消。欠番の穴埋め（読み取れなかった写真の位置を空文字で埋める）までは見送り、follow-up Issue #347で扱う
  - #4b（空imageReadingsのパーステストがバイパスされている）: **同コミットで解消**。`GeminiRecipeConsultProvider` を直接叩く新規テストファイル（3件）を追加。ソート処理を消すと2/3が赤くなることを変異検証済み
  - #4a（`MAX_CONSULT_IMAGES_PER_MESSAGE` の直接テストが定数をなぞっただけ）: follow-up Issue #347で扱う（UIでの実添付動作は今回のスコープ外）
  - #1〜#3: `wontfix`（外れ。上記根拠のとおり実装は正しい）
- 参照: PR https://github.com/keisato848/daidoko/pull/346
