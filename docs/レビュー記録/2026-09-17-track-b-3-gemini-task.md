# gemini-task track-b-3（相談画面へのUI配線）採否記録

- 日付: 2026-09-17
- 対象: `feat/consult-agent-actions` へのコミット `5e10523`
- 依頼票: `.scratch/track-b-3-ui-wiring-task.md`
- レポート: `.scratch/gemini-tasks/2026-09-17T13-24-58-736Z-track-b-3.md`

## モデル

枠切れで本来の write 役（`claude-opus-4-6-thinking`）ではなく `gemini-3.1-pro-high` に控えへ落ちた。
機械の検査（秘密混入・設定源変更）は通ったが、**差分の質は自分で読んで確かめる必要があった**
（このケースでは実際に問題があった。下記）。

## 差分を読んで見つけた問題（採用前に修正）

| #   | 症状                                                               | 原因                                                                                                                                                                                                                           | 対処                                                                             |
| --- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------- |
| 1   | 送信ボタンをタップすると実行時に例外になる型エラー（`tsc` で検出） | `send` のシグネチャを `() => Promise<void>` から `(overrideText?: string) => Promise<void>` に変えたのに、`onPress={send}` がそのまま残り、`GestureResponderEvent` が `overrideText` に渡る                                    | `onPress={() => void send()}` に修正                                             |
| 2   | 新規結合テスト4件中3件が実際に赤い（`npx jest` で確認）            | `getByText('recipeImport.consult.actionConfirm')` のように、i18n の**キー文字列そのもの**をアサートしていた。このプロジェクトの jest 設定は i18n をモックせず実際の `t()` を通すので、描画されるのは翻訳済み文言（「足す」等） | 実際の翻訳文言（「足す」「取り消す」「を買い物リストに足しました」等）に書き換え |
| 3   | lint エラー2件                                                     | 日本語ハードコード（候補選択時に送るテキストが `t()` を通っていない）・テストの `any`                                                                                                                                          | i18n キー `candidateChoice` を追加して置き換え、型注釈を追加                     |

## 検証

- `pnpm --filter mobile typecheck` / `lint`: 0 件
- `pnpm --filter mobile test`（フル）: 146 スイート / 1450 件 全通過（回帰なし）
- 変異検証: `send` ハンドラに「確認前に `confirmMutate` を呼ぶ」経路を一時的に仕込み、結合テスト4件中2件が赤くなることを確認 → 歯があることの裏付け

## 判断

機械の検査だけでは実際のバグ（クラッシュ・歯の無いテスト）を検出できなかった。
**差分を自分で読み、typecheck/lint/test を実際に走らせる**手順を省略しなかったことで発見できた。
今回のような「モデルが控えへ落ちたパス」は特に、レポートの「未検証・懸念点」欄の申告
（今回は node_modules ビルド失敗の話のみで、上記3件には言及が無かった）を鵜呑みにしない。
