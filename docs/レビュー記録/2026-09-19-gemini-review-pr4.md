# Gemini レビュー（PR-4・`feat/menu-slot-settings`）反証記録

- 日付: 2026-09-19
- 対象: `origin/main...HEAD`（`e55e3f7`〜`3e22961`・5 コミット）の中核差分（services / utils / db）
- 経路: `mcp__plugin_harness_gemini__ask_gemini`（gemini-3.1-pro-high・effort high・web 検索 8 回）
- 設問: Claude の現状認識 C1〜C9 を 当たり / 外れ / 未確認 で判定＋見落とし
- 注: `plugin/scripts/gemini-review.mjs` は `HARNESS_ROOT`（claude-harness 自身）に固定されていて
  プロジェクトの差分は見られない。誤って 1 回走らせた結果が
  `C:\Projects\claude-harness\docs\reviews\gemini-review-2026-09-19.md` に残っている（このリポジトリとは無関係）

## Gemini の判定と反証

| #           | 状態                       | 重大度 | 指摘                                                                         | 調査                                                                                                                                                                  | 判断                                                                             | 対応                                                                                |
| ----------- | -------------------------- | ------ | ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| C1          | 当たり                     | —      | menu_plan / cooking_log の enqueue が無かった → 差分で積むようにした         | `menu-plan.service.ts` `writeStoredMenuPlan` 末尾・`cooking-log.service.ts` の 2 箇所                                                                                 | 認識どおり                                                                       | 無し                                                                                |
| C2          | 当たり                     | —      | プラン id を毎回作り直していた → 使い回しに                                  | `const planId = ids[0] ?? generateId()`                                                                                                                               | 認識どおり                                                                       | 無し                                                                                |
| C3          | 当たり                     | —      | LWW の鍵 generatedAt → updatedAt（v21）・古い相手と互換                      | `menuPlanEffectiveUpdatedAt`・送受信・墓標の 3 箇所                                                                                                                   | 認識どおり                                                                       | 無し                                                                                |
| **C4**      | **外れ（私の説明が過大）** | 低     | 「バックフィルが無いと既存行が null で受信が常に勝つ」                       | `menuPlanEffectiveUpdatedAt` は null なら `generatedAt` へ倒すので、`incomingChangeWins` に null は渡らない。v15 の shopping_items とは違う                           | **Gemini が正しい。** バックフィルは正しさに必須ではなく、行を自己記述にする保険 | `migrate.ts` の docstring と `migrate.test.ts` のコメントを書き換え（`3e22961` 後） |
| C5          | 当たり                     | —      | `nextSlotId` は空き番号を使い回す・競合は LWW で収束・料理は消えない         | `menuSlots.ts`                                                                                                                                                        | 認識どおり                                                                       | 無し                                                                                |
| C6          | 当たり                     | —      | 定義の無い枠は料理のある日にだけ出す                                         | `buildWeekRows` の `unknownIds` 判定                                                                                                                                  | 認識どおり                                                                       | 無し                                                                                |
| C7          | 未確認 → 当たり            | 中     | 主菜を空にできない UI 抑止は差分外。サービスは `days` から日を消す経路を持つ | UI 側は `menu.tsx` の `canClear={… && slotId !== 'main'}` で確認済み（レビューには渡していない）                                                                      | 抑止は UI だけだった                                                             | **サービスにも止めを入れた**（`setMenuPlanSlotEntry` 冒頭で main + null は no-op）  |
| C8(c)       | 当たり                     | 中     | `saveMenuSlotSettings` の `onConflictDoUpdate.set` に `autoFill` が無い      | Drizzle は `set` に書いた列だけ更新（参照: orm.drizzle.team/docs/insert）                                                                                             | 潜在バグ。いま誰も autoFill を読まない                                           | PR-5a（autoFill を使う PR）で直す。§10.15 に記載済み                                |
| C8(a,b,d,e) | 未確認                     | —      | 差分外                                                                       | 自分で確認済み（`generateMenuPlan` は slots を引き継がない・`markDone` は days のみ・`hydrate` は days のみ・サーバー `infer.ts:478/525/858` は呼び出しごとに月次枠） | 認識どおり                                                                       | PR-5 の計画に反映済み                                                               |
| C9(a)       | 未確認 → 当たり            | —      | `BottomSheet` の KeyboardAvoider が他のモーダルを崩す可能性                  | エミュレータで `AppDialog`（献立を消す の確認）を目視確認済み（`docs/verification/1.13.2-10036.json`）                                                                | 崩れない                                                                         | 無し（実機 AQUOS でも見る）                                                         |
| C9(b)       | 当たり                     | —      | zod は未知キーを strip する（参照: zod.dev）                                 | —                                                                                                                                                                     | 古いクライアントは `updatedAt` を無視するだけ                                    | 無し                                                                                |

## 見落としの指摘

| #   | 状態             | 重大度 | 指摘                                                                                        | 調査                                                                                                                                                             | 判断                                       | 対応                                                |
| --- | ---------------- | ------ | ------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | --------------------------------------------------- |
| G1  | **当たり**       | 中     | `writeStoredMenuPlan` が delete → insert を素の直列で行い、途中で落ちると献立が丸ごと消える | v19 からずっとそう。`applyMenuPlan` 側は `db.transaction` を使っている                                                                                           | 直す価値あり（自分が触った関数）           | **`db.transaction` で囲んだ**。enqueue は commit 後 |
| G2  | **外れ**         | —      | 主菜を差し替えると `reason` と `doneAt` が「サイレントに消える」                            | `upsertSlotEntry` の docstring と §10.15 に**意図として**記載: 別の料理に変えたのに「済み」が残ると作っていない物が作ったことになる。`reason` は手で選んだので空 | 設計どおり。Gemini は意図を読んでいない    | wontfix（この記録で再議論を防ぐ）                   |
| G3  | 当たり（見送り） | 低     | enqueue がトランザクション外（commit 直後に落ちると一生積まれない）                         | `recipe.service` 等、既存の全サービスが同じ形。`enqueueSyncEntity` は `getDb()` を使うので tx に同乗できない。参加時の全量積み直しが救済                         | 既存パターンと同じ。単独で変える話ではない | 見送り。クラウド同期設計の課題として別途            |
| G4  | 外れ             | —      | 「adb reverse に root は不要」（web）                                                       | 私の主張は「`adb root` が adbd を再起動して reverse の規則を**消す**」で、必要性の話ではない                                                                     | 論点違い                                   | 無し（§4-0c の記述はそのまま）                      |

## 集計

採用 2（G1 トランザクション化・C7 サービス側の止め）／ 説明の訂正 1（C4）／ 見送り 2（G3・C8c は PR-5a へ）／ 外れ 2（G2・G4）

## 教訓

- 「なぜ必要か」を書くとき、**フォールバックが既にある経路**では前例（v15）の理屈をそのまま当てはめない。
  C4 は正しい対処に間違った理由を付けていた。理由が間違うと、次に読む人が前提を誤る
- 自分が触った関数の**既存の弱点**（トランザクション境界）は、差分にはなくてもレビューの対象になる。触ったなら見る
