# 在庫・レシート・名寄せ 設計レビュー（2026-08-15）

実機で出た2つの症状を起点に、`docs/買い物リスト・在庫設計.md` の §5.6（レシート登録）と
§6（食材名の正規化・突合）を読み直した記録。**結論から言うと、症状はどちらも実装ミスではなく
設計の穴**で、当該2節は書き直しが要る。この文書は差し替え案であり、合意できたら
`買い物リスト・在庫設計.md` に畳み込んで消す。

---

## 0. 先に訂正（この文書を書くきっかけになった誤り）

`買い物リスト・在庫設計.md` §5.6 に次の記述がある:

> ※ SDK 54 移行で ML Kit 依存を外したため、現行ビルドではネイティブモジュールが無く**この経路は事実上休止中**。

**これは事実ではない。** 端末内 OCR は生きている。

- `b2520c4`（2026-06-21）で unbundled ML Kit（Play Services 版）として配線し直されている
- `apps/mobile/plugins/withDaidokoOcr.js` が `play-services-mlkit-text-recognition-japanese` を
  `app/build.gradle` に注入し、`DaidokoOcrModule.kt` を生成し、`MainApplication.kt` に登録している
- `app.json` の `plugins` に `./plugins/withDaidokoOcr` が入っている

上の一文は #68（2026-07-12）で追記されたもので、**復活から3週間後に「休止中」と書かれた**。
症状2はまさにこの生きている経路が出しているので、記述を信じると原因に辿り着けない。

---

## 1. 症状と原因

### 症状1: 初回登録で名寄せの推論が走らなかった

**原因: 辞書を埋める処理の呼び出し元が「在庫で作れるレシピ」画面ひとつしかない。**

`resolvePantryNames()` を呼ぶのは `app/(tabs)/cookable.tsx` だけで、画面表示時に自動実行される。
在庫への登録（手入力・レシート・買い物リストからの移動）はどれも辞書に触らない。
**cookable を一度も開かなければ辞書は空のまま**で、これが「登録しても推論されない」の正体。

### 症状2: 初回のレシート読み取りがエラーになる（モジュールのダウンロード中らしき挙動）

**原因: 可用性の判定が実態を見ておらず、失敗時の逃げ道も無い。** 3つ重なっている。

1. `DaidokoOcrModule.kt` の `isAvailable()` が **無条件で `true` を返す**
   （`withDaidokoOcr.js` の Kotlin ソース内 `promise.resolve(true)`）
2. そもそも `receipt.tsx` は `isAvailable()` を呼んでいない。
   `createClientOcrRecognizer()` が「`NativeModules.DaidokoOcr` が存在するか」だけで判定し、
   存在すれば端末内 OCR 経路を選ぶ
3. ML Kit は unbundled 版なので**モデルは Play Services が必要に応じて配布する**。
   `com.google.mlkit.vision.DEPENDENCIES = ocr,ica,barcode_ui` の meta-data で
   インストール時ダウンロードを依頼してはいるが、これは**ベストエフォート**（インストール直後・
   通信が無い・Play Services が後回しにした、のいずれでも未取得のまま）

未取得のまま `recognizer.process()` を呼べば失敗し、`receipt.tsx` の catch が
**ML Kit の生のエラー文字列をそのまま画面に出す**。クラウド経路へのフォールバックは無い。

設計文書 §5.6 は「端末内 OCR が使える端末では優先し、無い端末ではクラウドへ自動切替」と
書いているが、**「モジュールはあるが今は使えない」という第三の状態を想定していない**。
現実にはこれが初回起動時の既定状態になる。

### 症状3（コードから発見・ユーザー未報告）: 表記違いが在庫に重複して積まれる

`pantry.service.ts` の upsert は `nameNormalized` の完全一致（＋単位一致）でマージ先を探し、
**辞書を一切参照しない**。`normalizeItemName` は NFKC（全半角）とカナ折り畳み（カタカナ→ひらがな）
までで、**漢字↔かなは吸収しない**。よって「人参」と「にんじん」は別レコードになる。
レシートからの一括追加はこの経路なので出やすい。

### 症状4（コードから発見）: 辞書が効いても片側しか寄らない

`utils/itemMatch.ts` は辞書を**両側**に当てる正しい作りになっている（`ca`/`cb`）。
しかし辞書に入るのは在庫側の名前だけなので、実際には片側しか canonical に移らない。

在庫「にんじん」・レシピ「人参」で、AI が canonical を「にんじん」と決めた場合:

|            | 元の名前 | 辞書適用後                       |
| ---------- | -------- | -------------------------------- |
| 在庫       | にんじん | にんじん                         |
| レシピ材料 | 人参     | **人参**（辞書に無いので素通り） |

一致しない。部分一致（3文字以上の包含）も効かない。逆に AI が「人参」を canonical に選べば
たまたま一致する。**AI が選んだ表記とレシピの表記が偶然揃ったときだけ効く**。

---

## 2. 設計として間違っていた点

| #   | 現行設計                                                                        | なぜ破綻するか                                                                                                                                |
| --- | ------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| A   | 名寄せ辞書を「**在庫名** → canonical」の辞書として定義した（§6）                | 突合は 在庫 × レシピ材料。片側だけ写しても同じ空間に来ない                                                                                    |
| B   | 辞書を埋めるのは「在庫で作れるレシピ」画面（AI 呼び出しの回数を抑える意図）     | 主要な入口（登録・レシート）で埋まらず、体感は「機能していない」                                                                              |
| C   | 辞書は**読むとき**だけ当てる（保存する名前は生のまま）                          | 在庫の中に表記違いが溜まる。辞書を直しても既存の重複は直らない                                                                                |
| D   | 端末内 OCR は「使える端末／使えない端末」の二値と想定                           | unbundled ML Kit は「使える端末だが今は未取得」がある。しかも初回がそれ                                                                       |
| E   | 端末内 OCR を既定・クラウドをフォールバックと位置づけた（無料・オフライン優先） | 品質が逆。端末内はヒューリスティックで品目名のみ、クラウドは構造化出力で非食品除外・重複統合まで済む。コストも思考オフ化で ¥0.35/件（PR#170） |

---

## 3. 再設計案

### 3.1 名寄せ: 「両側を canonical 空間へ写す」に改める

辞書の定義を**「任意の食材表記 → canonical」**に広げる（在庫名に限定しない）。
解決対象に**レシピの材料名**を含める。データモデル（`name_aliases`）は変更不要 —
`source_normalized` に何を入れてよいかの解釈を広げるだけで済む。

### 3.2 埋めるタイミング: 入口に移す

在庫への登録が確定した時点（手入力・レシート確定・買い物リストからの移動）でその場で解決する。
cookable 画面での一括解決は**取りこぼしの受け皿として残す**（辞書が空の既存ユーザー・
枠切れで後回しになった分）。

コスト面: 無料枠は現状 1日30件＋広告ボーナス30（上限90）。入口で投げるのは
**未解決の名前だけ**（`getUncachedNames`）なので、初回に集中して以降は逓減する。
レシピ材料名を対象に加える分だけ初回の件数は増える。

### 3.3 書き込み: canonical で寄せてから保存

`addPantryItem` のマージ先探索を canonical ベースにする。表示名は入力どおりを保ち、
突合キーだけ canonical にする（`pantry_items` に canonical 列を足すか、
`name_normalized` の意味を canonical に変えるかは実装時に決める。後者は移行が要る）。

既存の重複をどうするかは別途決める（§4）。

### 3.4 OCR: 「使えるか」を実際に確かめ、駄目ならクラウドへ落とす

最低限、次の2つは要る。

1. `isAvailable()` を**本当の可用性**にする。ML Kit の `ModuleInstallClient.areModulesAvailable()`
   でモデル取得済みかを問い合わせる。取得済みでなければ `false` を返す
2. 端末内 OCR が失敗したら**クラウド経路へ自動フォールバック**する。
   生の ML Kit エラーを画面に出さない

そのうえで、**優先順位そのものを反転させる**ことを推奨する。クラウドを既定、端末内 OCR を
オフライン時のフォールバックに降格する。理由は品質差（構造化出力 vs ヒューリスティック）と、
思考オフ後のコスト（¥0.35/件）が無視できる水準にあること。

畳む選択肢もある（§4）。

---

## 4. 決めてほしいこと

1. **端末内 OCR を残すか、畳むか。**
   残す = オフラインで動く価値／保守コスト（Kotlin・ML Kit・16KB アライン）を負い続ける。
   畳む = `withDaidokoOcr` プラグイン・`utils/receiptParser`・可用性判定が全部消えて単純になるが、
   通信必須になる。iOS の Apple Vision 実装（#58）の扱いも連動する。
2. **名寄せを入口で自動的に AI へ投げてよいか。** 体験は良くなるがコストと無料枠を消費する。
   投げない設計（辞書が育つのを待つ）は今と同じ症状を引きずる。
3. **既存の重複在庫をどうするか。** 放置／マージ用の UI を作る／canonical 再計算で一括統合。

---

## 5. 関連

- Issue #178（レシート読み込みを含む在庫機能の改善）— この文書の 3.1〜3.4 が具体案にあたる
- Issue #58（iOS OCR を Apple Vision で実装）— §4-1 の判断と連動
- 賞味期限は 2026-07-07 に取り下げ済み。**再提案しない**
