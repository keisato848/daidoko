# だいどこ — 多言語対応設計

> 作成: 2026-08-07
> ステータス: 設計（着手条件は §1）
> 前提: `docs/海外展開の検討.md`（なぜ英語圏か・なぜ EEA を外すか）

---

## 1. 着手条件（これが満たされるまで P2 以降を始めない）

**日本語版で再現ループが 1 周した実績。**

「店で食べた → 作った → 感想で近づけた」が、本人以外の利用者で最低 1 件。
摩擦の大きいループが実際に回るかは**言語に関係なく未検証**で、回らないなら英語化しても回らない。

例外は **P1（単位系）**。日本語版にも価値があるため着手条件を待たない。

**P0（アレルギー）は機能として実装しない**ことになった（§5）。免責と注意書きで対応済み。

---

## 2. 規模の実測（2026-08-07）

以前「2,883 箇所」と数えたが、**これはコメントと日本語処理ロジックを含む過大な数**だった。
実際に翻訳が要る表示文言は次のとおり。

| 層                         | 文字列数 |
| -------------------------- | -------- |
| 画面（`app/`）             | 428      |
| サービス（エラー文言など） | 170      |
| コンポーネント             | 36       |
| その他（agents ほか）      | 36       |
| ユーティリティ             | 22       |
| DB                         | 9        |
| **合計**                   | **701**  |

**除外 516**（翻訳ではなく「言語別実装」または「そもそも表示されない」）:

| 除外したもの                                 | 理由                                                                                                |
| -------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `db/seed.ts`                                 | サンプルデータ。フラグが無ければ表示されない                                                        |
| `e2e/photo-recipe-batch-fixtures.ts`         | 評価用の期待値。ユーザーには出ない                                                                  |
| `constants/licenses.ts`                      | OSS ライセンス表記。翻訳しない                                                                      |
| `services/recipe-photo-inference.service.ts` | 端末内フォールバックの**日本語辞書**（ラベル→料理名）。英語圏では作り直し                           |
| `utils/` の日本語処理群                      | `receiptParser` `recipeTextParser` `recipeTextNormalizer` `stepTimer` `itemMatch` `itemName` `kana` |

計測は `node scripts/agent/count-i18n-strings.mjs` で再現できる（`--files` でファイル別）。
**除外リストはスクリプト内にあり、判断の根拠をコメントで残している。**

> **701 は「自分で書ける」規模。** これが翻訳の外注や機械翻訳を選ばない根拠でもある（§9）。

---

## 3. 技術選定

### 3-1. ライブラリ: `expo-localization` + `i18n-js`

| 候補                            | 判断                                                                                   |
| ------------------------------- | -------------------------------------------------------------------------------------- |
| **expo-localization + i18n-js** | **採用。** Expo 公式で SDK 54 に追従。依存が軽く、必要なのは辞書引き・補間・複数形だけ |
| react-i18next                   | 見送り。React Context・Suspense・プラグイン機構は本アプリの規模に対して重い            |
| 自前実装                        | 見送り。**複数形と数値・日付の書式**を自前で持つ価値はない                             |

`expo-localization` は端末のロケールと**地域**（`region`）を返す。地域は単位系の既定
（§4）に使う。

### 3-2. 辞書の置き場と形

```
apps/mobile/src/i18n/
├── index.ts            # 初期化・t() のエクスポート・ロケール解決
├── locales/
│   ├── ja.ts
│   └── en.ts
└── __tests__/
    └── parity.test.ts  # ja と en のキーが一致することを検証
```

**JSON ではなく TypeScript** にする。理由は**キーの型を効かせるため**。
`ja.ts` を `as const` で書き、`en.ts` に同じ型を要求すれば、
**キーの抜けと打ち間違いがコンパイルで落ちる**。

```ts
// ja.ts
export default {
  recipe: {
    refine: {
      title: 'お店の味に近づける',
      feedbackLabel: '作ってみてどうだった？',
      hint: '味の方向（甘い・辛い・濃い・薄い）を書くほど直しやすくなります。',
    },
  },
} as const;

// en.ts
import type ja from './ja';
const en: typeof ja = {
  /* 抜けがあればここで型エラー */
};
```

### 3-3. キーの命名

`<領域>.<画面または機能>.<用途>`。例: `recipe.refine.title` / `common.error.offline`。

**画面のファイル構成に合わせる**（`app/(tabs)/recipes/[id]/refine.tsx` → `recipe.refine.*`）。
どのキーがどこで使われるかを、辞書だけ見て追えるようにするため。

### 3-4. 複数形と補間

日本語には複数形が無く、英語にはある。**実測で数値を含む文言は多い**
（「写真${n}枚」「最新${n}件」「今日あと${n}回」など）。

i18n-js の複数形機能を使い、**辞書側で吸収する**。

```ts
// ja.ts
photoCount: { one: '写真%{count}枚', other: '写真%{count}枚' },
// en.ts
photoCount: { one: '%{count} photo', other: '%{count} photos' },
```

**文字列連結で文を組み立てない。** 語順が言語で変わるため、文全体を1キーにする。

---

## 4. 単位系（P1）— 翻訳ではなく「正しさ」

AI は「180度」「大さじ1」を出す。**米国利用者に 180°F と読まれれば料理が失敗する。**
tbsp も英 15ml / 豪 20ml で違う。R2 は分量を具体値で変える機能なので、
**単位系が曖昧だと機能の根幹が壊れる**。

### 方針

| 対象         | 保存                         | 表示                                           |
| ------------ | ---------------------------- | ---------------------------------------------- |
| 分量         | **原文のまま**（変換しない） | 表示時に整形                                   |
| 温度         | 原文のまま                   | 表示時に ℃ ⇄ ℉                                 |
| 単位系の選択 | `app_meta` に保存            | 既定は端末の地域（US → imperial、他 → metric） |

**保存値を変換しない**のが要点。変換して保存すると、丸め誤差が版を重ねるたびに蓄積し、
`recipeDiff` が「変わっていないのに変わった」と表示する。R2 の安全装置を壊す。

### AI 側

プロンプトに**出力する単位系を明示する**。変換ではなくモデルに直接その単位で書かせる。
アプリ側の整形は既存レシピの表示用に留める。

---

## 5. アレルギー — 機能として実装しない（2026-08-08 決定）

**アレルゲンの検出・除去・警告は実装しない。** 免責（ポリシー）＋注意書きで対応する。

### なぜ実装しないのか

**保証できないものを、保証しているように見せる方が危険だから。**

AI 推論に基づいて「このレシピはナッツ不使用です」と示せば、外れたときに
**注意書きしかない場合より悪い結果**になる。誤った安心を与えるため。

材料は写真から判別できないもの（だし・油・調味料の原材料）を含み、
R2 では感想に応じて**材料が追加される**。この不確かさは仕組み上なくならない。

当初は「欧米では前提条件」として実装を計画したが、それは強すぎた。
**レシピアプリの一般的な扱いとしても、免責＋注意書きが標準**である。

### 実装したこと

| 場所                        | 内容                                                             |
| --------------------------- | ---------------------------------------------------------------- |
| `docs/privacy-policy.md` §7 | AI 生成物の免責。**アレルゲンの検出・警告を行わない旨を明記**    |
| 写真レシピの警告            | 「分量・手順」だけでなく**材料そのもの**の確認を促す文言に変更   |
| R2（お店の味に近づける）    | 差分プレビューに注意書きを追加。**材料が追加されうる**ことを明示 |

**R2 は写真レシピより強い注意を出す。** 材料を増やす経路であり、
「もっとコクを」という感想で乳製品が足されるといったことが起きるため。

### 英語版で必要になること

免責の**英訳**。§8 の P2（文字列外部化）と同時に行う。
ポリシーの英語版も P4（ストア掲載）に含める。

---

## 6. AI プロンプトの英語化（P3）

サーバーの5ファイル（`vision-recipe` / `recipe-refine` / `meal-vision` /
`receipt-vision` / `name-resolve`）は**全文日本語**で、日本の家庭料理を前提にしている。

「家庭の台所で作れる形に翻訳する」の中身が
「**日本のスーパー**で買えるもので代用」「中華レンジではなく家庭のコンロ」である。
英語化は文字列置換ではなく、**対象国ごとの前提の書き換え**になる。

### 手順

1. リクエストに `locale` を追加（既定 `ja`。**省略時の挙動は現状のまま**）
2. プロンプトを変種として持つ（`buildSystemPrompt(variant, locale)`）
3. **R0 の評価ハーネス（`vision-eval.ts`）を英語で回して品質を測る**
4. 日本語版と同じ合格ラインを満たすまで公開しない

**3 を飛ばさない。** 日本語で合格したプロンプトの直訳が英語で同じ品質になる保証はない。

---

## 7. 海外版で出さない機能

`docs/海外展開の検討.md` §4 の結論。**翻訳しない＝機能ごと出さない。**

| 機能           | 理由                             |
| -------------- | -------------------------------- |
| JAN バーコード | 日本の商品コード                 |
| レシート OCR   | 日本のレシート書式・半角カナ前提 |
| 名寄せ辞書     | 日本語の辞書                     |

在庫・買い物リストの一群がまるごと日本結合だが、**主役ではないので外せる**。
ロケールで出し分ける（`androidOnly` と同じ仕組みを `jaOnly` として一般化する）。

---

## 8. 段階と成果物

| 段階   | 成果物                                                                    | 着手条件                     |
| ------ | ------------------------------------------------------------------------- | ---------------------------- |
| ~~P0~~ | ~~アレルギー・食事制限~~ → **実装しない**。免責＋注意書きで対応済み（§5） | 完了                         |
| **P1** | 単位系の抽象化（保存は原文・表示で整形・地域で既定を決める）              | **なし**（日本語版にも価値） |
| **P2** | i18n 基盤 ＋ 文字列外部化（701 件）＋ 新規の埋め込み防止                  | §1 の着手条件                |
| **P3** | AI プロンプトの英語版 ＋ **R0 を英語で再測定**                            | P2                           |
| **P4** | ストア掲載の英語版・**アイコン差し替え**・配信国の設定                    | P3・ユーザー承認             |
| **P5** | 検索・名寄せの言語対応                                                    | 需要が出てから               |

### P2 の進め方

**一括置換をしない。** 701 件を一度に動かすと、レビューできない差分になる。

1. 基盤を入れ、**1 画面だけ**移行して型と複数形の使い勝手を確かめる
2. 画面単位で移行（`app/` 428 件 → 32 ファイル）
3. サービス層のエラー文言（170 件）
4. **移行が終わった領域から順に、日本語リテラルの新規追加を lint で禁止する**

最後の項目が重要。**放置すると外部化した端から増える。**
ESLint の `no-restricted-syntax` で、移行済みディレクトリの JSX テキストと
文字列リテラルに日本語を含むものを検出する。

---

## 9. やらないこと

- **翻訳の外注・機械翻訳の一括適用** — 701 件は自分で書ける規模で、
  文言はプロダクトの声そのもの（「深夜の私設レシピ蔵書庫」）。品質を落とさない
- **日本語版の文言変更を伴う整理** — i18n はキーを外に出すだけ。
  文言を良くするのは別の作業として分ける（差分が読めなくなる）
- **RTL（アラビア語等）対応** — 対象言語に無い

---

## 10. 関連

- 判断の前提: `docs/海外展開の検討.md`
- AI 品質の測り方: `docs/レシピ推論の評価設計.md`
- ブランド（アイコンの例外規定）: `docs/brand/ロゴ仕様.md`
